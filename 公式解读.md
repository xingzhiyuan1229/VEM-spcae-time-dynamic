# 公式解读：空间-时间虚元方法求解弹性动力学

> 对应论文：*Space-time virtual element method for elastodynamics: Theory, applications, and code development*  
> 作者：Bing-Bing Xu, Philipp Junker, Peter Wriggers

---

## 目录

1. [引言与背景](#1-引言与背景)  
2. [变分公式推导（第2节）](#2-变分公式推导)  
   - 2.1 [基于 Hamilton 原理的变分公式](#21-基于-hamilton-原理的变分公式)  
   - 2.2 [空间-时间双线性格式与时间迎风稳定化](#22-空间-时间双线性格式与时间迎风稳定化)  
3. [空间-时间虚元方法（第3节）](#3-空间-时间虚元方法)  
   - 3.1 [网格假设](#31-网格假设)  
   - 3.2 [投影算子](#32-投影算子)  
   - 3.3 [空间域离散双线性形式](#33-空间域离散双线性形式)  
   - 3.4 [线性空间-时间弹性矩阵形式](#34-线性空间-时间弹性矩阵形式)  
4. [有限元方法对比（第4节）](#4-有限元方法对比)  
5. [数值算例概述（第5节）](#5-数值算例概述)  
6. [结论](#6-结论)

---

## 1. 引言与背景

本文研究的核心问题是：如何对二维弹性动力学问题进行**空间-时间联合离散**。传统方法（如 Newmark 时间积分）将时间与空间分离处理，而本文将时间视为与空间坐标平行的额外维度，在"时间-空间柱"上统一离散。

**虚元方法（VEM）** 是有限元方法（FEM）向任意多边形/多面体单元的推广。其核心思想是：不显式计算多边形上的形函数，而是将虚元空间中的函数通过**投影算子**映射到多项式空间中进行计算。

本文的技术路线（作者如何做）：
1. 从 **Hamilton 最小作用量原理** 出发推导变分形式（弱形式）。  
2. 引入速度 $\boldsymbol{v}$ 为独立变量，得到两场弱形式方程。  
3. 加入**时间迎风稳定化**项，保证数值稳定性。  
4. 对空间域采用 **VEM（k=1阶）** 离散，时间方向采用 **线性有限元（Lagrange 插值）** 离散。  
5. 利用空间-时间网格的张量积结构，借助 **Kronecker 积** 高效组装整体刚度矩阵。

---

## 2. 变分公式推导

### 2.1 基于 Hamilton 原理的变分公式

#### 公式 (1)：空间-时间柱

$$Q \coloneqq \Omega \times I \tag{1}$$

**说明：** $\Omega \subset \mathbb{R}^n$（$n=1,2,3$）是空间域，$I=(0,T)$ 是时间区间。"空间-时间柱" $Q$ 将时间看作与空间坐标同等地位的维度，是整个 space-time 离散的物理舞台。

---

#### 公式 (2)：Hamilton 作用量

$$H \coloneqq \int_{\Omega}\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}\boldsymbol{\rho}\cdot \mathrm{d}\boldsymbol{u}\,\mathrm{d}V = \int_{\Omega}\int_{0}^{T}\rho\,\nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}t\,\mathrm{d}V = \int_{I}\int_{\Omega}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V\,\mathrm{d}t = \int_{I}2\mathcal{K}\,\mathrm{d}t \tag{2}$$

**严格推导：**

> **数学事实 1（动量定义）：** 线性动量密度向量定义为 $\boldsymbol{\rho} \coloneqq \rho\,\nabla_t\boldsymbol{u}$，其中 $\rho$ 是质量密度，$\nabla_t = \frac{\mathrm{d}}{\mathrm{d}t}$ 是时间导数（即速度 $\dot{\boldsymbol{u}}$）。

**第一个等号（动量路径积分 → 时间积分）：**

$$\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}\boldsymbol{\rho}\cdot \mathrm{d}\boldsymbol{u} = \int_0^T \boldsymbol{\rho}\cdot\frac{\mathrm{d}\boldsymbol{u}}{\mathrm{d}t}\,\mathrm{d}t = \int_0^T \rho\,\nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}t$$

这里用了**链式法则**：路径积分 $\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}(\cdot)\,\mathrm{d}\boldsymbol{u}$ 参数化为对时间的积分，$\mathrm{d}\boldsymbol{u} = \nabla_t\boldsymbol{u}\,\mathrm{d}t$。代入 $\boldsymbol{\rho} = \rho\nabla_t\boldsymbol{u}$，得到被积项 $\rho(\nabla_t\boldsymbol{u})^2$。

**第二个等号（交换积分次序）：**

$$\int_{\Omega}\int_0^T\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}t\,\mathrm{d}V = \int_I\int_{\Omega}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V\,\mathrm{d}t$$

> **数学事实 2（Fubini 定理）：** 在满足可积条件时，二重积分的积分次序可以交换。

**第三个等号（引入动能）：**

$$\int_I 2\mathcal{K}\,\mathrm{d}t \quad \text{其中} \quad \mathcal{K} = \int_{\Omega}\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V$$

因此 $\int_{\Omega}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V = 2\mathcal{K}$，即作用量 $H$ 等于动能对时间积分的两倍。

---

#### 公式 (3)：动能

$$\mathcal{K} = \int_{\Omega}\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V \tag{3}$$

**说明：** 这是连续介质力学中的标准动能表达式。$\|\nabla_t\boldsymbol{u}\|^2 = \dot{\boldsymbol{u}}\cdot\dot{\boldsymbol{u}}$ 是速度的平方模，乘以质量密度 $\rho$ 后在体积上积分，再乘以 $\frac{1}{2}$，得到系统的总动能。

---

#### 公式 (4)：能量守恒（热力学第一定律）

$$\int_I\mathcal{K}\,\mathrm{d}t + \int_I\mathcal{G}\,\mathrm{d}t = 0 \tag{4}$$

**说明：**

> **数学事实 3（热力学第一定律）：** 对于保守系统，动能与势能之和（即总机械能）在时间上的变化为零（能量守恒）。

这里将时间积分形式写为：系统动能历程积分加上总势能历程积分等于零，表达了保守弹性系统的能量平衡。$\mathcal{G}$ 是总势能（见公式 (5)）。此式也是 Hamilton 原理的出发点：真实运动使作用量 $H = \int_I(\mathcal{K}-\mathcal{G})\,\mathrm{d}t$ 取驻值。

---

#### 公式 (5)：总势能

$$\mathcal{G} \coloneqq \int_{\Omega}\psi\,\mathrm{d}V - \int_{\Omega}\bar{\boldsymbol{f}}\cdot\boldsymbol{u}\,\mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\boldsymbol{u}\,\mathrm{d}\Gamma \tag{5}$$

**说明：**  
总势能 $\mathcal{G}$ 由三部分组成：
- $\int_{\Omega}\psi\,\mathrm{d}V$：**弹性应变能**（内能），$\psi$ 是自由能密度（见公式 (6)）。  
- $-\int_{\Omega}\bar{\boldsymbol{f}}\cdot\boldsymbol{u}\,\mathrm{d}V$：**体力功的负值**，$\bar{\boldsymbol{f}}$ 是体积力。  
- $-\int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\boldsymbol{u}\,\mathrm{d}\Gamma$：**Neumann 边界（$\Gamma_\sigma$）上面力做功的负值**，$\bar{\boldsymbol{t}}$ 是面力（外压）。  

势能 = 弹性能 - 外力功，这是连续介质力学变分原理的标准形式。

---

#### 公式 (6)：自由能密度（应变能密度）

$$\psi \coloneqq \frac{\lambda}{2}\left(\operatorname{tr}(\boldsymbol{\varepsilon})\right)^2 + \mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon} = \frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} \tag{6}$$

**说明：**  
这是**线弹性材料**的应变能密度，也称为 Lamé 型能量密度。

> **数学事实 4（线弹性本构关系）：** 对于各向同性线弹性材料，弹性张量 $\mathbb{D}$ 由两个 Lamé 常数 $\lambda$（第一 Lamé 系数）和 $\mu$（剪切模量）完全确定。

**等号关系推导：**

由公式 (7) 的弹性张量，

$$\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \mathbb{D}_{ijkl}\varepsilon_{ij}\varepsilon_{kl} = \lambda\delta_{ij}\delta_{kl}\varepsilon_{ij}\varepsilon_{kl} + \mu(\delta_{ik}\delta_{jl}+\delta_{il}\delta_{jk})\varepsilon_{ij}\varepsilon_{kl}$$

利用 $\delta_{ij}\varepsilon_{ij} = \operatorname{tr}(\boldsymbol{\varepsilon})$ 以及对称性 $\varepsilon_{ij}=\varepsilon_{ji}$：

$$\lambda(\operatorname{tr}(\boldsymbol{\varepsilon}))^2 + \mu(\varepsilon_{ik}\varepsilon_{ik}+\varepsilon_{il}\varepsilon_{li}) = \lambda(\operatorname{tr}(\boldsymbol{\varepsilon}))^2 + 2\mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon}$$

因此 $\frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \frac{\lambda}{2}(\operatorname{tr}(\boldsymbol{\varepsilon}))^2 + \mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon}$，等号成立。

---

#### 公式 (7)：弹性张量分量

$$\mathbb{D}_{ijkl} = \lambda\,\delta_{ij}\delta_{kl} + \mu\left(\delta_{ik}\delta_{jl}+\delta_{il}\delta_{jk}\right) \tag{7}$$

**说明：**  
这是**各向同性线弹性材料**弹性张量的标准 Lamé 表示。

> **数学事实 5（各向同性张量的表示定理）：** 四阶各向同性张量可以用 Kronecker δ 的乘积线性表示。最一般形式包含三个独立标量，但对于弹性张量，由对称性约束只剩两个（$\lambda$ 和 $\mu$）。

$\delta_{ij}$ 是 Kronecker delta，满足：$\delta_{ij}=1$ 若 $i=j$，否则为 $0$。该公式确保了弹性张量满足：
- **主对称性**：$\mathbb{D}_{ijkl} = \mathbb{D}_{klij}$  
- **次对称性**：$\mathbb{D}_{ijkl} = \mathbb{D}_{ijlk} = \mathbb{D}_{jikl}$  

---

#### 公式 (8)：应变张量（对称梯度）

$$\boldsymbol{\varepsilon} = \frac{1}{2}\left(\nabla\boldsymbol{u}+\nabla\boldsymbol{u}^T\right) =: \nabla^{\mathrm{sym}}\boldsymbol{u} = \boldsymbol{\varepsilon}(\boldsymbol{u}) \tag{8}$$

**说明：**  
这是**小应变假设**下的几何方程（应变-位移关系）。

> **数学事实 6（线性化应变）：** 在小变形假设下，Green-Lagrange 应变张量线性化为对称梯度：$\varepsilon_{ij} = \frac{1}{2}(u_{i,j}+u_{j,i})$，其中逗号表示偏导。

这里 $\nabla\boldsymbol{u}$ 是位移梯度张量（$(\nabla\boldsymbol{u})_{ij} = \partial u_i/\partial x_j$），对其取转置后与自身相加再除以2，得到**对称**的应变张量，消去了刚体转动分量。

---

#### 公式 (9)：Hamilton 作用量的重新表述

$$H = \int_I(\mathcal{K}-\mathcal{G})\,\mathrm{d}t \tag{9}$$

**说明：**  
这是 **Hamilton 最小作用量原理**的标准形式：作用量 $H$ 等于 Lagrange 量 $\mathcal{L} = \mathcal{K}-\mathcal{G}$（动能减势能）对时间的积分。由公式 (4) 即 $\int_I\mathcal{K}\,\mathrm{d}t + \int_I\mathcal{G}\,\mathrm{d}t = 0$ 以及对 $H=\int_I 2\mathcal{K}\,\mathrm{d}t$ 的变换可得此式。

---

#### 公式 (10)：混合形式的动能（引入速度为独立变量）

$$\mathcal{K} = \int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2\,\mathrm{d}\Omega \tag{10}$$

**严格推导：**

这是一个经典的**Lagrange 乘子 / 混合变分**处理技巧。将速度 $\boldsymbol{v}$ 作为与位移 $\boldsymbol{u}$ 独立的变量引入，通过以下构造使两者在驻值条件下相等。

**验证等价性：** 当约束 $\boldsymbol{v} \equiv \nabla_t\boldsymbol{u}$（即速度等于位移的时间导数）时，

$$\rho\,\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2 = \rho\,\nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2 = \frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2$$

对域积分后恰好退化为原动能公式 (3)。等价性成立。

> **数学事实 7（鞍点变分原理）：** 混合形式的作用量对 $(\boldsymbol{v}, \boldsymbol{u})$ 取驻值，等价于：  
> (a) 对 $\boldsymbol{v}$ 取驻值给出运动学约束 $\boldsymbol{v} = \nabla_t\boldsymbol{u}$；  
> (b) 对 $\boldsymbol{u}$ 取驻值给出运动方程。  
> 这与 Hellinger-Reissner 原理的思路一致。

---

#### 公式 (11)：Hamilton 驻值条件

$$H = H[\boldsymbol{v},\boldsymbol{u},\nabla^{\mathrm{sym}}\boldsymbol{u},\nabla_t\boldsymbol{u}] \to \mathrm{stat} \iff \delta H = \delta_{\boldsymbol{v}}H + \delta_{\boldsymbol{u}}H = 0 \quad \forall\,\delta\boldsymbol{v},\delta\boldsymbol{u} \tag{11}$$

**说明：**  
Hamilton 原理要求作用量 $H$ 对所有容许变分 $\delta\boldsymbol{v}$、$\delta\boldsymbol{u}$ 取驻值（一阶变分为零）。由于 $\boldsymbol{v}$ 和 $\boldsymbol{u}$ 是两个独立场，总变分分解为对各自的变分之和：$\delta H = \delta_{\boldsymbol{v}}H + \delta_{\boldsymbol{u}}H = 0$。

---

#### 公式 (12)：独立驻值条件

$$\begin{cases}\delta_{\boldsymbol{v}}H = 0 & \forall\,\delta\boldsymbol{v} \\ \delta_{\boldsymbol{u}}H = 0 & \forall\,\delta\boldsymbol{u}\end{cases} \tag{12}$$

**说明：**  
> **数学事实 8（变分独立性）：** 因为 $\delta\boldsymbol{v}$ 和 $\delta\boldsymbol{u}$ 是独立任意的变分，$\delta H=0$ 必须对它们各自独立成立。这是混合变分原理推导两场方程的标准步骤。

---

#### 公式 (13)：变分展开

$$\begin{cases}\delta_{\boldsymbol{v}}H = \displaystyle\int_I\int_{\Omega}\rho(\nabla_t\boldsymbol{u}-\boldsymbol{v})\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\[6pt] \delta_{\boldsymbol{u}}H = \displaystyle\int_I\int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t - \int_I\!\left(\int_{\Omega}\bigl(\boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u})-\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\bigr)\mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{u}\end{cases} \tag{13}$$

**严格推导（$\delta_{\boldsymbol{v}}H$）：**

将 $H$ 对混合动能 $\mathcal{K}$（见公式 (10)）关于 $\boldsymbol{v}$ 求变分：

$$\delta_{\boldsymbol{v}}\mathcal{K} = \int_{\Omega}\rho\,\delta\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \rho\,\boldsymbol{v}\cdot\delta\boldsymbol{v}\,\mathrm{d}\Omega = \int_{\Omega}\rho(\nabla_t\boldsymbol{u}-\boldsymbol{v})\cdot\delta\boldsymbol{v}\,\mathrm{d}\Omega$$

注意势能 $\mathcal{G}$ 不含 $\boldsymbol{v}$，故 $\delta_{\boldsymbol{v}}\mathcal{G}=0$。对时间积分后得第一式。

> **物理意义：** $\delta_{\boldsymbol{v}}H=0$ 对任意 $\delta\boldsymbol{v}$ 成立等价于 $\boldsymbol{v}=\nabla_t\boldsymbol{u}$（速度约束）。

**严格推导（$\delta_{\boldsymbol{u}}H$）：**

对 $H=\int_I(\mathcal{K}-\mathcal{G})\,\mathrm{d}t$ 关于 $\boldsymbol{u}$ 求变分：

$$\delta_{\boldsymbol{u}}\mathcal{K} = \int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t(\delta\boldsymbol{u})\,\mathrm{d}\Omega$$

（$\boldsymbol{v}$ 是独立变量，不随 $\boldsymbol{u}$ 变化；对 $\nabla_t\boldsymbol{u}$ 的变分为 $\nabla_t(\delta\boldsymbol{u})$）。

对势能 $\mathcal{G}$ 求变分（利用公式 (5) 和 (6)）：

$$\delta_{\boldsymbol{u}}\mathcal{G} = \int_{\Omega}\boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u})\,\mathrm{d}V - \int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma$$

其中应力 $\boldsymbol{\sigma}=\partial\psi/\partial\boldsymbol{\varepsilon}=\mathbb{D}:\boldsymbol{\varepsilon}$（本构关系）。

> **数学事实 9（自由能对应变的导数 = 应力）：** 对于 $\psi=\frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon}$，有 $\partial\psi/\partial\varepsilon_{ij}=\mathbb{D}_{ijkl}\varepsilon_{kl}=\sigma_{ij}$。

将 $\delta_{\boldsymbol{u}}H = \delta_{\boldsymbol{u}}(\mathcal{K}-\mathcal{G})$ 对时间积分后得第二式。

---

#### 公式 (14)：分部积分后的弱形式

$$\begin{cases}\displaystyle\int_I\int_{\Omega}-\rho\,\boldsymbol{v}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t + \int_I\int_{\Omega}\rho\,\nabla_t\boldsymbol{u}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\[8pt] \displaystyle\int_I\int_{\Omega}\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t + \int_I\int_{\Omega}\boldsymbol{\sigma}:\delta\nabla^{\mathrm{sym}}\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t = \int_I\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}V + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t\end{cases} \tag{14}$$

**严格推导（对第二式中含 $\nabla_t(\delta\boldsymbol{u})$ 的项进行分部积分）：**

对公式 (13) 第二式中的第一项：

$$\int_I\int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t(\delta\boldsymbol{u})\,\mathrm{d}V\,\mathrm{d}t$$

> **数学事实 10（时间分部积分）：** $\int_0^T f\,\dot{g}\,\mathrm{d}t = [fg]_0^T - \int_0^T \dot{f}\,g\,\mathrm{d}t$

对时间方向进行分部积分：

$$\int_I\rho\,\boldsymbol{v}\cdot\nabla_t(\delta\boldsymbol{u})\,\mathrm{d}t = \Big[\rho\,\boldsymbol{v}\cdot\delta\boldsymbol{u}\Big]_0^T - \int_I\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}t$$

利用**边界条件** $\delta\boldsymbol{u}|_0 = \delta\boldsymbol{u}|_T \equiv \boldsymbol{0}$（变分在时间端点为零，这是 Hamilton 原理的标准假设），边界项消失：

$$\Big[\rho\,\boldsymbol{v}\cdot\delta\boldsymbol{u}\Big]_0^T = 0$$

因此：

$$\int_I\int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t(\delta\boldsymbol{u})\,\mathrm{d}V\,\mathrm{d}t = -\int_I\int_{\Omega}\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t$$

对公式 (13) 第二式整理，将负号移到右边，即得公式 (14) 第二式。

> **物理意义：** 公式 (14) 第二式是**弱形式的动量方程**，对应经典的 $\rho\ddot{\boldsymbol{u}} = \nabla\cdot\boldsymbol{\sigma} + \bar{\boldsymbol{f}}$（当对 $\delta\boldsymbol{u}$ 用分部积分后可恢复强形式）。边界条件（初始位移、初始速度）可以直接施加，无需额外修改。

---

### 2.2 空间-时间双线性格式与时间迎风稳定化

#### 公式 (15)：双线性格式

$$A(\boldsymbol{v},\boldsymbol{u},\delta\boldsymbol{v},\delta\boldsymbol{u}) = \begin{pmatrix}A_{vv}(\delta\boldsymbol{v},\boldsymbol{v}) + A_{vu}(\delta\boldsymbol{v},\boldsymbol{u})\\A_{uv}(\delta\boldsymbol{u},\boldsymbol{v}) + A_{uu}(\delta\boldsymbol{u},\boldsymbol{u})\end{pmatrix} = \begin{pmatrix}\mathbf{0}\\l(\delta\boldsymbol{u})\end{pmatrix} \tag{15}$$

**说明：** 这是公式 (14) 的**紧凑双线性算子写法**。四个子双线性形式分别对应：速度场自耦合、速度与位移耦合（两个方向）、位移场弹性刚度。右端项只有位移方程有载荷项 $l(\delta\boldsymbol{u})$（见公式 (17)）；速度方程右端为零。

---

#### 公式 (16)：四个子双线性形式

$$\begin{aligned}A_{vv}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_0^T\int_{\Omega}-\rho\,\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{vu}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_0^T\int_{\Omega}\rho\,\delta\boldsymbol{v}\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{uv}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_0^T\int_{\Omega}\rho\,\delta\boldsymbol{u}\cdot\nabla_t\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{uu}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_0^T\int_{\Omega}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t.\end{aligned} \tag{16}$$

**说明：** 这四个双线性形式直接从公式 (14) 读取，是对弱形式各项的分类命名。$A_{vv}$ 来源于速度方程的质量项（负号源自混合动能中的 $-\frac{1}{2}\rho\|\boldsymbol{v}\|^2$），$A_{vu}$ 来自速度方程中的 $\nabla_t\boldsymbol{u}$ 耦合项，$A_{uv}$ 来自分部积分后的惯性项，$A_{uu}$ 是弹性刚度双线性形式。

---

#### 公式 (17)：载荷线性形式

$$l(\delta\boldsymbol{u}) = \int_0^T\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t \tag{17}$$

**说明：** 这是标准的**等效节点力**线性泛函，对应弱形式右端的体力和边界面力项。

---

#### 公式 (18)：时间迎风稳定化后的双线性形式

$$\begin{aligned}A_{vv}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_0^T\int_{\Omega}-\rho\,(\delta\boldsymbol{v}+\theta h\,\nabla_t\delta\boldsymbol{v})\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{vu}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_0^T\int_{\Omega}\rho\,(\delta\boldsymbol{v}+\theta h\,\nabla_t\delta\boldsymbol{v})\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{uv}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_0^T\int_{\Omega}\rho\,(\delta\boldsymbol{u}+\theta h\,\nabla_t\delta\boldsymbol{u})\cdot\nabla_t\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t,\\A_{uu}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_0^T\int_{\Omega}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}+\theta h\,\nabla_t\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t.\end{aligned} \tag{18}$$

**说明（时间迎风稳定化）：**

不加稳定化的公式 (16) 对某些问题会导致**数值振荡（不稳定）**。时间迎风方案将测试函数（变分）替换为：

$$\delta\boldsymbol{u} \;\to\; \delta\boldsymbol{u} + \theta h\,\nabla_t(\delta\boldsymbol{u}), \qquad \delta\boldsymbol{v} \;\to\; \delta\boldsymbol{v} + \theta h\,\nabla_t(\delta\boldsymbol{v})$$

其中：
- $\theta > 0$ 是**稳定化参数**（通常取 $\theta=0.01$，越大越稳定但引入数值耗散）。  
- $h$ 是单元在时间方向的长度（时间步长 $\Delta t$）。  
- $\nabla_t(\delta\boldsymbol{u}) = \partial(\delta\boldsymbol{u})/\partial t$ 是测试函数的时间导数。  

> **数学事实 11（Petrov-Galerkin 迎风方法）：** 迎风稳定化的思想来自有限元方法中处理对流占主导方程（如平流-扩散方程）的 SUPG（Streamline Upwind Petrov-Galerkin）方法。将测试函数加上与流动方向一致的扰动项，可以抑制非物理振荡。此处"时间迎风"即对时间方向的对流算子做相同处理。

附加项 $\theta h\,\nabla_t(\cdot)$ 相当于在原格式上叠加了一个正的数值耗散，使离散格式更接近**迎风差分**的特性，从而保证数值稳定性。

---

#### 公式 (19)：稳定化后的载荷形式

$$l(\delta\boldsymbol{u}) = \int_0^T\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot(\delta\boldsymbol{u}+\theta h\,\nabla_t\delta\boldsymbol{u})\,\mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot(\delta\boldsymbol{u}+\theta h\,\nabla_t\delta\boldsymbol{u})\,\mathrm{d}\Gamma\right)\mathrm{d}t \tag{19}$$

**说明：** 稳定化后测试函数同样替换，因此载荷线性形式也需对应修改，以保持 Galerkin 一致性。

---

## 3. 空间-时间虚元方法

### 3.1 网格假设

论文采用**时间方向张量积网格**：空间域 $\Omega$ 被划分为不重叠多边形集合 $\{\mathcal{T}_h^x\}$，时间区间 $[0,T]$ 被划分为 $N_T$ 个子区间 $\mathcal{K}_{t_n}:=(t_n,t_{n+1})$。每个空间-时间单元为 $\mathcal{K}=\mathcal{K}_x\times\mathcal{K}_t$（张量积）。

此结构的关键优势：上下面（$t=t_n$ 和 $t=t_{n+1}$ 处的多边形截面）形状完全相同，因此可以分别计算空间积分和时间积分，最后用 **Kronecker 积**组合，大幅简化编程。

---

### 3.2 投影算子

#### 公式 (20)：$H^1$ Ritz 投影算子定义

$$a_E(w - \Pi_\nabla^{k,E}w,\,q_k) = 0 \quad \forall\,q_k\in P_k(E) \tag{20}$$

**严格推导与说明：**

$\Pi_\nabla^{k,E}: H^1(E)\to P_k(E)$ 是**能量正交投影（Ritz 投影）**。

> **数学事实 12（Ritz 投影）：** 给定 Hilbert 空间 $V$ 和其子空间 $W$，元素 $w\in V$ 在 $W$ 上关于内积 $a(\cdot,\cdot)$ 的 Ritz 投影 $\Pi w$ 定义为：$a(w-\Pi w,\,q)=0\;\forall\,q\in W$。这等价于在 $a$-度量意义下寻找 $W$ 中最接近 $w$ 的元素。

这里 $a_E(p,q)=\int_E\nabla p\cdot\nabla q\,\mathrm{d}\Omega$（即 Laplace 算子对应的双线性形式）。公式 (20) 说明：投影的误差 $w-\Pi_\nabla^{k,E}w$ 与所有 $k$ 阶多项式 $q_k$ 在 $a_E$ 意义下正交。

这一投影的计算结果将用于离散化 $A_{uu}$（弹性刚度项）。

---

#### 公式 (21)：投影算子的额外约束（固定常数）

$$\begin{cases}\dfrac{1}{N_V^E}\displaystyle\sum_{i=1}^{N_V^E}(\Pi_\nabla^{k,E}w-w) = 0, & k=1, \\[8pt] \dfrac{1}{|E|}\displaystyle\int_E(\Pi_\nabla^{k,E}w-w)\,\mathrm{d}\Omega = 0, & k>1.\end{cases} \tag{21}$$

**说明：**  
公式 (20) 仅确定投影在"梯度意义下"的分量，而常数部分（零阶多项式）处于 $a_E$ 的核中（$\nabla c=0$），因此需要额外条件来唯一确定投影。

> **数学事实 13（约束消歧义）：** $\nabla p = \nabla q \Rightarrow p-q=c$（常数）。因此 $a_E(\cdot,\cdot)$ 对常数函数不提供约束，需附加正规化条件。

- **$k=1$ 时：** 投影在所有顶点处的平均值等于原函数在顶点处的平均值（使用顶点 DOF 的平均值作为约束）。  
- **$k>1$ 时：** 投影在单元上的积分平均等于原函数的积分平均。

这两个条件对应不同阶次的 VEM 实现方式，确保了投影的唯一性。

---

#### 公式 (22)：虚元空间 $\mathcal{V}_h^E$ 的定义

$$\mathcal{V}_h^E := \{v\in H^1(E):\,v|_{\partial E}\in\mathcal{B}(\partial E),\;\Delta v\in P_{k-2}(E)\} \tag{22}$$

**说明：**  
这是 VEM 的核心定义。虚元空间中的函数满足：
- 在边界 $\partial E$ 上属于边界元空间 $\mathcal{B}(\partial E)$（见公式 (23)）。  
- 在单元内部，其 Laplace 算子 $\Delta v$ 属于 $k-2$ 阶多项式空间（**此即"虚"的含义**：内部函数不需显式计算，只要求其满足特定的偏微分方程特征）。

对于 $k=1$：条件为 $\Delta v\in P_{-1}(E) = \{0\}$，即 $v$ 是调和函数（满足 Laplace 方程），但在内部不需要求值。

> **数学事实 14（虚元方法的可计算性）：** VEM 的关键性质是：虽然基函数 $\phi$ 在单元内部不能显式表达，但其投影 $\Pi_\nabla^{k,E}\phi$ 完全由边界 DOF 决定，因此刚度矩阵的一致部分是可计算的。

---

#### 公式 (23)：边界元空间

$$\mathcal{B}(\partial E) := \{v\in C^0(\partial E):\,v_F\in P_k(F),\;\forall\,F\in\partial E\} \tag{23}$$

**说明：** 边界上的函数在每条边 $F$ 上是 $k$ 阶多项式，且在整个边界上连续（$C^0$）。对于 $k=1$，每条边上是线性函数，由两端顶点值确定。这使得相邻单元之间的连续性通过共享顶点和边自动满足。

---

#### 公式 (24)：函数在基底下的展开

$$v_h = \boldsymbol{\phi}^T\hat{v}_h, \quad \Pi_\nabla^{k,E}v_h = \boldsymbol{m}_k^T\Pi_\nabla^{k,E}\hat{v}_h = \boldsymbol{\phi}^T\mathbf{\Pi}_\nabla^{k,E}\hat{v}_h \tag{24}$$

**说明：**  
- $\boldsymbol{\phi}$ 是虚元空间 $\mathcal{V}_h^E$ 的（虚）基函数向量。  
- $\boldsymbol{m}_k$ 是 $k$ 阶单项式基底（即 $P_k(E)$ 的基），例如 $k=1$ 时 $\boldsymbol{m}_1 = [1,\,(x-x_E)/h_E,\,(y-y_E)/h_E]^T$（归一化的单项式）。  
- $\hat{v}_h$ 是节点自由度向量。  
- $\mathbf{\Pi}_\nabla^{k,E}$ 是投影算子的**矩阵表示**（将节点 DOF 映射到多项式系数）。  

> **数学事实 15（矩阵化投影算子）：** 由线性代数，有限维空间上的线性算子唯一对应矩阵。将投影条件 (20)-(21) 写成关于节点 DOF 的线性方程组，可以显式求解 $\mathbf{\Pi}_\nabla^{k,E}$。

---

#### 公式 (25)：$L^2$ 正交投影算子定义

$$\left(v-\Pi_{k,E}^0 v,\,q_k\right)_E = 0, \quad \forall\,q_k\in P_k(E) \tag{25}$$

**说明：**  
$\Pi_{k,E}^0: L^2(E)\to P_k(E)$ 是标准的 **$L^2$ 正交投影**，其中 $(f,g)_E=\int_E f\,g\,\mathrm{d}\Omega$。

> **数学事实 16（$L^2$ 投影）：** $\Pi_{k,E}^0 v$ 是在 $P_k(E)$ 中最小化 $\|v-p\|_{L^2(E)}^2$ 的多项式，由正规方程 $\int_E(v-\Pi_{k,E}^0 v)q_k\,\mathrm{d}\Omega=0$ 确定。

这一投影用于计算动能（质量矩阵）相关项（公式 (16)/(18) 中的 $A_{vv}$、$A_{vu}$、$A_{uv}$）。  
**重要结论**（论文指出）：对 $k\le 2$，$\Pi_\nabla^{k,E}=\Pi_{k,E}^0$，因此本文（$k=1$）无需单独计算两套投影。

---

### 3.3 空间域离散双线性形式

#### 公式 (26)：（论文原文编号为 (27)）近似双线性形式

$$\begin{aligned}a_h^E(p_h,q_h) &:= a^E(\Pi_\nabla^{k,E}p_h,\Pi_\nabla^{k,E}q_h) + S^E((\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h),\\m_h^E(p_h,q_h) &:= m^E(\Pi_{k,E}^0 p_h,\Pi_{k,E}^0 q_h) + R^E((\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{q}_h).\end{aligned} \tag{27}$$

**说明（VEM 离散双线性形式的核心构造）：**

VEM 的单元双线性形式由两部分组成：

**（a）一致性项（Consistency term）：**
$$a^E(\Pi_\nabla^{k,E}p_h,\Pi_\nabla^{k,E}q_h)$$
这部分对精确解可以精确还原。

> **数学事实 17（多项式精确性）：** 若 $p_h,q_h\in P_k(E)$（精确多项式），则 $\Pi_\nabla^{k,E}p_h=p_h$，因此一致性项等于精确值。

**（b）稳定化项（Stabilization term）：**
$$S^E((\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h)$$
这部分对投影后的"核空间"分量进行稳定化，防止秩亏（虚元空间中不被投影捕捉的自由度需要控制，否则刚度矩阵奇异）。

> **数学事实 18（稳定化必要性）：** 若 $p_h\in\ker(\Pi_\nabla^{k,E})$（即 $\Pi_\nabla^{k,E}p_h=0$），仅有一致性项时 $a_h^E(p_h,p_h)=0$，导致刚度矩阵奇异。稳定化项确保 $a_h^E(p_h,p_h)\ge 0$ 且等于零当且仅当 $p_h=0$（在 DOF 意义下）。

---

#### 公式 (28)：$H^1$ 投影双线性形式的矩阵化

$$\begin{aligned}a_h^E(p,q) &= \int_{\mathcal{K}_x}(\nabla\Pi_\nabla^{k,E}p_h)\cdot(\nabla\Pi_\nabla^{k,E}q_h)\,\mathrm{d}\Omega + S^E((\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h)\\&= \hat{p}_h^T\left[(\mathbf{\Pi}_\nabla^{k,E*})^T\!\int_{\mathcal{K}_x}\nabla\boldsymbol{m}_k\nabla\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^{k,E*} + (\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\right]\hat{q}_h\\&= \hat{p}_h^T(\mathbf{K}_c^\nabla+\mathbf{K}_s^\nabla)\hat{q}_h = \hat{p}_h^T\mathbf{K}_\nabla\hat{q}_h.\end{aligned} \tag{28}$$

**严格推导：**

**第一步：展开投影。** 由公式 (24)，$\Pi_\nabla^{k,E}p_h = \boldsymbol{m}_k^T\mathbf{\Pi}_\nabla^{k,E*}\hat{p}_h$，其中 $\mathbf{\Pi}_\nabla^{k,E*}$ 是矩阵，将节点 DOF 映射到多项式系数。则：

$$\nabla\Pi_\nabla^{k,E}p_h = (\nabla\boldsymbol{m}_k^T)\mathbf{\Pi}_\nabla^{k,E*}\hat{p}_h$$

**第二步：代入积分。**

$$\int_{\mathcal{K}_x}(\nabla\Pi_\nabla^{k,E}p_h)\cdot(\nabla\Pi_\nabla^{k,E}q_h)\,\mathrm{d}\Omega = \hat{p}_h^T(\mathbf{\Pi}_\nabla^{k,E*})^T\underbrace{\int_{\mathcal{K}_x}\nabla\boldsymbol{m}_k(\nabla\boldsymbol{m}_k)^T\,\mathrm{d}\Omega}_{\text{可计算！}}\mathbf{\Pi}_\nabla^{k,E*}\hat{q}_h$$

> **数学事实 19（多项式梯度可精确积分）：** $\nabla\boldsymbol{m}_k$ 是多项式，因此 $\int_E\nabla\boldsymbol{m}_k(\nabla\boldsymbol{m}_k)^T\,\mathrm{d}\Omega$ 完全可由 Gauss 积分精确计算。这就是 VEM 中"只需计算多项式的积分"的核心优势。

**第三步：定义一致性刚度矩阵。**
$$\mathbf{K}_c^\nabla := (\mathbf{\Pi}_\nabla^{k,E*})^T\int_{\mathcal{K}_x}\nabla\boldsymbol{m}_k\nabla\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^{k,E*}$$

**第四步：稳定化项的矩阵化。** 论文选取 $S^E((\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h) = \hat{p}_h^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h$，令：
$$\mathbf{K}_s^\nabla := (\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})$$

最终 $\mathbf{K}_\nabla = \mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla$，这是标量场的 VEM 刚度矩阵。

---

#### 公式 (29)：$L^2$ 投影质量形式的矩阵化

$$\begin{aligned}m_h^E(p,q) &= \int_{\mathcal{K}_x}(\Pi_{k,E}^0 p_h)(\Pi_{k,E}^0 q_h)\,\mathrm{d}\Omega + R^E((\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{q}_h)\\&= \hat{p}_h^T\left[(\mathbf{\Pi}_{k,E}^{0*})^T\int_{\mathcal{K}_x}\boldsymbol{m}_k\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_{k,E}^{0*} + |E|(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)^T(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\right]\hat{q}_h\\&= \hat{p}_h^T(\mathbf{K}_c^0+\mathbf{K}_s^0)\hat{q}_h = \hat{p}_h^T\mathbf{K}_0\hat{q}_h.\end{aligned} \tag{29}$$

**说明：** 推导结构与公式 (28) 完全平行，将梯度换为函数本身。稳定化参数选为 $|E|$（单元面积），以使 $\mathbf{K}_s^0$ 在量级上与 $\mathbf{K}_c^0$ 匹配。

---

#### 公式 (30)：$L^2$ 质量矩阵各分量

$$\begin{aligned}\mathbf{K}_c^0 &:= (\mathbf{\Pi}_{k,E}^{0*})^T\int_{\mathcal{K}_x}\boldsymbol{m}_k\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_{k,E}^{0*},\\\mathbf{K}_s^0 &:= |E|(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)^T(\mathbf{I}-\mathbf{\Pi}_{k,E}^0),\\\mathbf{K}_0 &:= \mathbf{K}_c^0+\mathbf{K}_s^0.\end{aligned} \tag{30}$$

**说明：** $\mathbf{K}_0$ 是标量场的 VEM 质量矩阵。$\mathbf{K}_c^0$ 一致部分通过多项式质量矩阵计算，$\mathbf{K}_s^0$ 稳定化部分用 $|E|$ 做量纲归一化。

---

#### 公式 (31)：$H^1$ 刚度矩阵各分量

$$\begin{aligned}\mathbf{K}_c^\nabla &:= (\mathbf{\Pi}_\nabla^{k,E*})^T\int_{\mathcal{K}_x}\nabla\boldsymbol{m}(\nabla\boldsymbol{m})^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^{k,E*},\\\mathbf{K}_s^\nabla &:= (\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E}),\\\mathbf{K}_\nabla &:= \mathbf{K}_c^\nabla+\mathbf{K}_s^\nabla.\end{aligned} \tag{31}$$

**说明：** $\mathbf{K}_\nabla$ 是标量场 Laplace 算子对应的 VEM 刚度矩阵，这与公式 (28) 完全对应，此处单独列出定义方便后续引用。

---

#### 公式 (32)：向量场分量写法

$$\boldsymbol{u} = [u_x,u_y]^T, \quad \delta\boldsymbol{u} = [\delta u_x,\delta u_y]^T \tag{32}$$

**说明：** 在二维问题中，位移向量和其变分各有两个分量，对应 $x$ 和 $y$ 方向。VEM 对每个分量分别处理（类似 FEM 的分量形式）。

---

#### 公式 (33)：向量场速度的双线性形式（块矩阵）

$$m_h^E(\delta\boldsymbol{v},\boldsymbol{v}) = \int_{\mathcal{K}_x}\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega = \begin{bmatrix}\delta\hat{v}_x^T & \delta\hat{v}_y^T\end{bmatrix}\begin{bmatrix}\mathbf{K}_0 & \\ & \mathbf{K}_0\end{bmatrix}\begin{bmatrix}\hat{v}_x\\\hat{v}_y\end{bmatrix} = \delta\hat{\boldsymbol{v}}^T\mathbb{K}_0\hat{\boldsymbol{v}}, \quad \mathbb{K}_0:=\begin{bmatrix}\mathbf{K}_0 & \\ & \mathbf{K}_0\end{bmatrix} \tag{33}$$

**说明：** 向量场速度的质量双线性形式 $\int\delta\boldsymbol{v}\cdot\boldsymbol{v} = \int\delta v_x v_x + \int\delta v_y v_y$，由于两分量解耦（各向同性且无耦合），形成**块对角矩阵** $\mathbb{K}_0=\mathrm{diag}(\mathbf{K}_0,\mathbf{K}_0)$。

---

#### 公式 (34)：Voigt 符号下的应变

$$\tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u}) \tag{34}$$

**说明：** **Voigt 记号**将对称二阶张量 $\boldsymbol{\varepsilon}$ 转换为向量形式，便于矩阵计算。见公式 (35) 的具体定义。

---

#### 公式 (35)：Voigt 变换矩阵和位移梯度向量

$$\mathbf{A} = \begin{bmatrix}1 & 0 & 0 & 0\\0 & 0 & 0 & 1\\0 & 1 & 1 & 0\end{bmatrix}, \quad \boldsymbol{d}(\boldsymbol{u}) = \left[\frac{\partial u_x}{\partial x},\;\frac{\partial u_x}{\partial y},\;\frac{\partial u_y}{\partial x},\;\frac{\partial u_y}{\partial y}\right]^T \tag{35}$$

**说明：** $\boldsymbol{d}(\boldsymbol{u})$ 包含位移梯度的所有四个分量。矩阵 $\mathbf{A}$ 将其组合为 Voigt 应变向量：

$$\tilde{\boldsymbol{\varepsilon}} = \begin{bmatrix}\varepsilon_{xx}\\\varepsilon_{yy}\\2\varepsilon_{xy}\end{bmatrix} = \begin{bmatrix}\partial u_x/\partial x\\\partial u_y/\partial y\\\partial u_x/\partial y + \partial u_y/\partial x\end{bmatrix}$$

其中 $\varepsilon_{xy}=\frac{1}{2}(\partial u_x/\partial y + \partial u_y/\partial x)$，Voigt 记号中工程剪应变为 $2\varepsilon_{xy}$，故 $\mathbf{A}$ 的第三行为 $[0,1,1,0]$。

> **数学事实 20（Voigt 记号）：** 利用 Voigt 记号，张量收缩 $\boldsymbol{\sigma}:\boldsymbol{\varepsilon}=\tilde{\boldsymbol{\sigma}}^T\tilde{\boldsymbol{\varepsilon}}$，弹性张量 $\mathbb{D}$ 的 Voigt 形式 $\tilde{\mathbb{D}}$ 是一个 $3\times 3$ 对称矩阵（平面应力/应变问题）。

---

#### 公式 (36)：向量场的投影展开

$$\Pi_\nabla^{k,E}\boldsymbol{v}_h = \begin{bmatrix}\boldsymbol{m}^T & \\ & \boldsymbol{m}^T\end{bmatrix}\begin{bmatrix}\mathbf{\Pi}_\nabla^{k,E*} & \\ & \mathbf{\Pi}_\nabla^{k,E*}\end{bmatrix}\begin{bmatrix}\hat{u}_h^x\\\hat{u}_h^y\end{bmatrix} = \mathbf{M}^T\mathbf{\Pi}_\nabla^*\hat{\boldsymbol{u}}_h \tag{36}$$

**说明：** 向量场的 Ritz 投影按分量分别进行，对 $x$ 和 $y$ 分量使用同一投影矩阵 $\mathbf{\Pi}_\nabla^{k,E*}$，组合成块对角形式。$\mathbf{M}$ 和 $\mathbf{\Pi}_\nabla^*$ 是对应的块矩阵。

---

#### 公式 (37)：弹性刚度双线性形式的矩阵化

$$\begin{aligned}a_h^E(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_{\mathcal{K}_x}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega = \int_{\mathcal{K}_x}\boldsymbol{d}(\delta\boldsymbol{u})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})\,\mathrm{d}\Omega\\&= \delta\hat{\boldsymbol{u}}^T\!\left((\mathbf{\Pi}_\nabla^*)^T\!\int_{\mathcal{K}_x}\nabla\mathbf{M}\,\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\nabla\mathbf{M}^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^* + \gamma\begin{bmatrix}\mathbf{K}_s^\nabla & \\ & \mathbf{K}_s^\nabla\end{bmatrix}\right)\hat{\boldsymbol{u}}\\&= \delta\hat{\boldsymbol{u}}^T(\mathbf{K}_c^\varepsilon+\gamma\mathbf{K}_s^\varepsilon)\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^T\mathbf{K}_\varepsilon\hat{\boldsymbol{u}}.\end{aligned} \tag{37}$$

**严格推导：**

**第一步：Voigt 变换。** 由公式 (34)，$\boldsymbol{\varepsilon}(\boldsymbol{u}) = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})$，于是：
$$\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u}) = \tilde{\boldsymbol{\varepsilon}}(\delta\boldsymbol{u})^T\tilde{\mathbb{D}}\,\tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = (\mathbf{A}\,\boldsymbol{d}(\delta\boldsymbol{u}))^T\tilde{\mathbb{D}}(\mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})) = \boldsymbol{d}(\delta\boldsymbol{u})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})$$

**第二步：用投影替换。** 由于仅对投影部分可以精确计算（与公式 (28) 类似），
$$\boldsymbol{d}(\boldsymbol{u}) \approx \boldsymbol{d}(\Pi_\nabla^{k,E}\boldsymbol{u}) = \nabla\mathbf{M}^T\mathbf{\Pi}_\nabla^*\hat{\boldsymbol{u}}$$

其中 $\nabla\mathbf{M}$ 是基底函数梯度的矩阵（可精确由多项式计算）。

**第三步：代入并识别矩阵。**
$$\mathbf{K}_c^\varepsilon := (\mathbf{\Pi}_\nabla^*)^T\int_{\mathcal{K}_x}\nabla\mathbf{M}\,\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\nabla\mathbf{M}^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^*$$

**稳定化项：** 论文取
$$\gamma\mathbf{K}_s^\varepsilon := \gamma\begin{bmatrix}\mathbf{K}_s^\nabla & \\ & \mathbf{K}_s^\nabla\end{bmatrix}$$
其中稳定化参数 $\gamma = \frac{1}{4N_K}\mathrm{tr}(\mathbf{K}_c^\varepsilon)$，$N_K$ 是单元顶点数。此选择使稳定化项与一致性项量级相近。

> **数学事实 21（稳定化参数选取）：** 对于线弹性问题，稳定化参数的选取对结果影响较小（参数不敏感性），这是 VEM 的一个实用优点。

最终 $\mathbf{K}_\varepsilon = \mathbf{K}_c^\varepsilon + \gamma\mathbf{K}_s^\varepsilon$ 是空间域弹性刚度矩阵，将用于张量积组合。

---

### 3.4 线性空间-时间弹性矩阵形式

#### 公式 (38)：张量积形函数

$$\Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x^b}\Phi_{\kappa_t}(1)\\\Phi_{\kappa_x^t}\Phi_{\kappa_t}(2)\end{bmatrix} \tag{38}$$

**说明：** 空间-时间单元 $\kappa=\kappa_x\times\kappa_t$ 的形函数是**空间形函数与时间形函数的张量积**。  
- $\Phi_{\kappa_x^b} = \Phi_{\kappa_x^t} =: \Phi_{\kappa_x}$：上下面形状相同（张量积假设），VEM 基函数。  
- $\Phi_{\kappa_t}(1)$，$\Phi_{\kappa_t}(2)$：时间方向的两个线性 Lagrange 形函数（见公式 (39)）。  
- 形函数向量的组织方式：先底面 DOF，后顶面 DOF。

---

#### 公式 (39)：时间方向线性形函数

$$\Phi_{\kappa_t}(1) = \frac{1-\xi}{2}, \quad \Phi_{\kappa_t}(2) = \frac{1+\xi}{2} \tag{39}$$

**说明：** 这是**标准一维线性 Lagrange 插值函数**，在参考区间 $\xi\in[-1,1]$ 上定义。

> **数学事实 22（等参变换）：** 物理时间区间 $[t_n,t_{n+1}]$ 通过线性等参映射 $t=\frac{t_n+t_{n+1}}{2}+\frac{h}{2}\xi$（$h=t_{n+1}-t_n$）映射到参考区间 $[-1,1]$，Jacobian 为 $\mathrm{d}t/\mathrm{d}\xi=h/2$。

- $\Phi_{\kappa_t}(1)|_{\xi=-1}=1$，$\Phi_{\kappa_t}(1)|_{\xi=1}=0$：对应 $t=t_n$（时间步的起点/底面）。  
- $\Phi_{\kappa_t}(2)|_{\xi=-1}=0$，$\Phi_{\kappa_t}(2)|_{\xi=1}=1$：对应 $t=t_{n+1}$（时间步的终点/顶面）。

---

#### 公式 (40)：张量积形函数的梯度

$$\nabla_x\Phi_\kappa = \begin{bmatrix}\nabla_x\Phi_{\kappa_x^b}\Phi_{\kappa_t}(1)\\\nabla_x\Phi_{\kappa_x^t}\Phi_{\kappa_t}(2)\end{bmatrix}, \quad \nabla_t\Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x^b}\nabla_t\Phi_{\kappa_t}(1)\\\Phi_{\kappa_x^t}\nabla_t\Phi_{\kappa_t}(2)\end{bmatrix} \tag{40}$$

**说明：** 应用**乘积法则**对张量积形函数分别对空间和时间求导：  
- 空间梯度：时间形函数作为常数因子，空间形函数求梯度。  
- 时间导数：空间形函数作为常数因子，时间形函数求导。

> **数学事实 23（乘积法则 / 分离变量）：** 对张量积函数 $f(x)\cdot g(t)$：$\partial_x[f(x)g(t)]=g(t)\partial_x f(x)$，$\partial_t[f(x)g(t)]=f(x)\partial_t g(t)$。

时间导数 $\nabla_t\Phi_{\kappa_t}(1) = -\frac{1}{h}$，$\nabla_t\Phi_{\kappa_t}(2) = \frac{1}{h}$（经等参变换：$\partial_t = \frac{2}{h}\partial_\xi$，故 $\partial_t\Phi_{\kappa_t}(1)=\frac{2}{h}\cdot(-\frac{1}{2})=-\frac{1}{h}$）。

---

#### 公式 (41)：空间-时间单元位移插值

$$U_h = \begin{bmatrix}\Phi_K^T & \\ & \Phi_K^T\end{bmatrix}\hat{U}_h =: \boldsymbol{\Phi}_K^T\hat{U}_h \tag{41}$$

**说明：** 空间-时间单元上位移向量 $\boldsymbol{U}=[U_x,U_y]^T$ 的插值。$\boldsymbol{\Phi}_K^T$ 是块对角形函数矩阵，$\hat{U}_h$ 是单元所有节点的自由度向量（见公式 (42)）。

---

#### 公式 (42)：形函数矩阵和自由度向量

$$\boldsymbol{\Phi}_K^T := \begin{bmatrix}\Phi_K^T & \\ & \Phi_K^T\end{bmatrix}, \quad \hat{U}_h := \begin{bmatrix}\hat{u}_x^b & \hat{u}_x^t & \hat{u}_y^b & \hat{u}_y^t\end{bmatrix}^T \tag{42}$$

**说明：** 自由度向量 $\hat{U}_h$ 按以下顺序排列：$x$ 方向底面 DOF、$x$ 方向顶面 DOF、$y$ 方向底面 DOF、$y$ 方向顶面 DOF。这与张量积结构直接对应。

---

#### 公式 (43)：单元刚度矩阵块结构

$$\mathbf{K} := \begin{bmatrix}\mathbf{K}_{vv} & \mathbf{K}_{vu}\\\mathbf{K}_{uv} & \mathbf{K}_{uu}\end{bmatrix} \tag{43}$$

**说明：** 整体单元刚度矩阵对应两场问题：$\boldsymbol{v}$（速度）和 $\boldsymbol{u}$（位移）各对应一个分块，共四个子块。离散方程为：

$$\begin{bmatrix}\mathbf{K}_{vv} & \mathbf{K}_{vu}\\\mathbf{K}_{uv} & \mathbf{K}_{uu}\end{bmatrix}\begin{bmatrix}\hat{V}\\\hat{U}\end{bmatrix} = \begin{bmatrix}\mathbf{0}\\\hat{F}\end{bmatrix}$$

---

#### 公式 (44)：$\mathbf{K}_{vv}$ 的 Kronecker 积表达

$$\begin{aligned}\mathbf{K}_{vv} &= \rho^{-1}\int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}(\Phi_\mathcal{K}+\theta h\nabla_t\Phi_\mathcal{K})\Phi_\mathcal{K}^T\,\mathrm{d}\Omega\,\mathrm{d}t\\&= \rho^{-1}\begin{bmatrix}\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega & \\ & \int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega\end{bmatrix}\\&\quad+\rho^{-1}\theta h\begin{bmatrix}\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega & \\ & \int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega\end{bmatrix}.\end{aligned} \tag{44}$$

**严格推导（Kronecker 积的来源）：**

由公式 (18)，$A_{vv}(\delta\boldsymbol{v},\boldsymbol{v})=-\rho^{-1}\int\int(\delta\boldsymbol{v}+\theta h\nabla_t\delta\boldsymbol{v})\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t$（注意论文用 $-$ 号因混合形式）。

考虑 $x$ 分量，形函数为 $\Phi_\kappa = \Phi_{\kappa_x}\otimes\Phi_{\kappa_t}$（张量积）。于是：

$$\int_{\mathcal{K}}\Phi_\kappa\Phi_\kappa^T\,\mathrm{d}\Omega\,\mathrm{d}t = \int_{\mathcal{K}_x}\Phi_{\kappa_x}\Phi_{\kappa_x}^T\,\mathrm{d}\Omega\otimes\int_{\mathcal{K}_t}\Phi_{\kappa_t}\Phi_{\kappa_t}^T\,\mathrm{d}t$$

> **数学事实 24（Kronecker 积与张量积积分的关系）：** 若 $f(\boldsymbol{x},t)=f_x(\boldsymbol{x})f_t(t)$（可分离变量），则：
> $$\int_{\kappa_x\times\kappa_t}f_x(\boldsymbol{x})f_x(\boldsymbol{x})^T f_t(t)f_t(t)^T\,\mathrm{d}\Omega\,\mathrm{d}t = \left(\int_{\kappa_x}f_x f_x^T\,\mathrm{d}\Omega\right)\otimes\left(\int_{\kappa_t}f_t f_t^T\,\mathrm{d}t\right)$$
> 注意这里用了 Kronecker 积的混合乘积性质：$(A\otimes B)(C\otimes D)=(AC)\otimes(BD)$。

稳定化项包含 $\theta h\nabla_t\Phi_\kappa = \theta h\,\Phi_{\kappa_x}\otimes\nabla_t\Phi_{\kappa_t}$，积分后得含 $\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}$ 的 Kronecker 积项。

---

#### 公式 (45)：空间积分项等于 $\mathbf{K}_0$

$$\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega = \mathbf{K}_c^0+\mathbf{K}_s^0 =: \mathbf{K}_0 \tag{45}$$

**说明：** 这将空间积分部分与之前在公式 (29)-(30) 中已经计算好的 VEM 质量矩阵 $\mathbf{K}_0$ 直接联系起来。张量积结构的核心优势在于：空间积分部分可以**复用**已有的 2D VEM 计算结果，无需重新对 3D 时-空单元进行积分。

---

#### 公式 (46)-(49)：时间方向积分矩阵

$$\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^1\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^2 w(i)\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{L} \tag{46}$$

$$\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^1\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^2 w(i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{Q} \tag{47}$$

$$\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^1\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^2 w(i)\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{R} \tag{48}$$

$$\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^1\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^2 w(i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{W} \tag{49}$$

**严格推导与显式计算：**

首先进行等参变换：$t=t_n+\frac{h}{2}(1+\xi)$，$\mathrm{d}t=\frac{h}{2}\mathrm{d}\xi$，积分区间 $[t_n,t_{n+1}]\to[-1,1]$，故有 $\frac{h}{2}$ 因子。

> **数学事实 25（两点 Gauss-Legendre 积分）：** 对于 $[-1,1]$ 上的多项式，两点 Gauss-Legendre 积分精确到三阶多项式：积分点 $\xi_1=-1/\sqrt{3}$，$\xi_2=1/\sqrt{3}$，权重 $w(1)=w(2)=1$。

由公式 (39)：$\boldsymbol{\phi}_{\mathcal{K}_t}(\xi)=[(1-\xi)/2,(1+\xi)/2]^T$，
$\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t} = \frac{\partial\boldsymbol{\phi}_{\mathcal{K}_t}}{\partial t}=\frac{2}{h}\frac{\partial\boldsymbol{\phi}_{\mathcal{K}_t}}{\partial\xi}=\frac{2}{h}\cdot[-1/2,1/2]^T=[-1/h,1/h]^T$。

**显式计算 $\mathbf{L}$：**

$$\mathbf{L} = \frac{h}{2}\int_{-1}^1\begin{bmatrix}\frac{(1-\xi)^2}{4} & \frac{1-\xi^2}{4}\\\frac{1-\xi^2}{4} & \frac{(1+\xi)^2}{4}\end{bmatrix}\mathrm{d}\xi = \frac{h}{2}\begin{bmatrix}2/3 & 1/3\\1/3 & 2/3\end{bmatrix} = \frac{h}{6}\begin{bmatrix}2 & 1\\1 & 2\end{bmatrix}$$

这正是一维线性单元的标准**质量矩阵**。

**显式计算 $\mathbf{Q}$ 和 $\mathbf{R}$（不对称！）：**

$$\mathbf{Q} = \frac{h}{2}\int_{-1}^1\begin{bmatrix}-1/h\\1/h\end{bmatrix}\begin{bmatrix}\frac{1-\xi}{2} & \frac{1+\xi}{2}\end{bmatrix}\mathrm{d}\xi = \frac{h}{2}\cdot\frac{1}{h}\begin{bmatrix}-1 & -1\\1 & 1\end{bmatrix}\cdot\frac{1}{2}\cdot 2 = \frac{1}{2}\begin{bmatrix}-1 & -1\\1 & 1\end{bmatrix}$$

$$\mathbf{R} = \mathbf{Q}^T = \frac{1}{2}\begin{bmatrix}-1 & 1\\-1 & 1\end{bmatrix}$$

注意 $\mathbf{Q}\ne\mathbf{R}^T$ 并非对称矩阵（因为一个含 $\nabla_t$、另一个不含），这正是时间迎风方案不对称的来源。

**显式计算 $\mathbf{W}$（刚度矩阵）：**

$$\mathbf{W} = \frac{h}{2}\int_{-1}^1\begin{bmatrix}1/h^2 & -1/h^2\\-1/h^2 & 1/h^2\end{bmatrix}\mathrm{d}\xi = \frac{1}{h}\begin{bmatrix}1 & -1\\-1 & 1\end{bmatrix}$$

这是一维线性单元的标准**刚度矩阵**。

---

#### 公式 (50)：块矩阵符号约定

$$\mathbf{K}_{any} = \begin{bmatrix}\mathbf{K}_{any}^{I,I} & \mathbf{K}_{any}^{I,II}\\\mathbf{K}_{any}^{II,I} & \mathbf{K}_{any}^{II,II}\end{bmatrix} \tag{50}$$

**说明：** 将矩阵 $\mathbf{K}_{any}$ 按 $x$（分量I）和 $y$（分量II）方向分成四个块，便于后续分量写法。

---

#### 公式 (51)：$\mathbf{K}_{vv}$ 的最终 Kronecker 积形式

$$\mathbf{K}_{vv} = \rho^{-1}\begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{L}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \rho^{-1}\theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{Q}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{51}$$

**说明：** 将公式 (44) 的时间积分用 $\mathbf{L}$、$\mathbf{Q}$（公式 (46)-(47)）替换，将空间积分用 $\mathbf{K}_0$（公式 (45)）替换，得到完全展开的 Kronecker 积形式。

- 第一项：普通质量项，$\mathbf{L}\otimes\mathbf{K}_0$（时间质量矩阵 $\times$ 空间质量矩阵）。  
- 第二项：稳定化惯性项，$\theta h\mathbf{Q}\otimes\mathbf{K}_0$（含时间梯度的混合项）。

---

#### 公式 (52)：$\mathbf{K}_{vu}$ 矩阵

$$\mathbf{K}_{vu} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{52}$$

**说明：** 对应 $A_{vu}(\delta\boldsymbol{v},\boldsymbol{u})$，含 $\nabla_t\boldsymbol{u}$（位移时间导数）乘以测试速度 $\delta\boldsymbol{v}$。时间方向积分含 $\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T = \mathbf{R}$（公式 (48)），稳定化项含 $\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T = \mathbf{W}$（公式 (49)）。

---

#### 公式 (53)：$\mathbf{K}_{uv}$ 矩阵

$$\mathbf{K}_{uv} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{53}$$

**说明：** 对应 $A_{uv}(\delta\boldsymbol{u},\boldsymbol{v})$，含 $\nabla_t\boldsymbol{v}$（速度时间导数）乘以测试位移 $\delta\boldsymbol{u}$（稳定化后）。注意 $\mathbf{K}_{vu}=\mathbf{K}_{uv}$ 在此处结构相同，但物理意义不同（一个是 $\delta\boldsymbol{v}$ 方程，另一个是 $\delta\boldsymbol{u}$ 方程）。

> **注意：** 此处 $\mathbf{K}_{uv}$ 的表达式与 $\mathbf{K}_{vu}$ 形式相同，这反映了时间分部积分后两个耦合项的对称性（尽管整体矩阵不一定对称）。

---

#### 公式 (54)：$\mathbf{K}_{uu}$ 矩阵

$$\mathbf{K}_{uu} = \begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,II}\\\mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,II}\\\mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix} \tag{54}$$

**说明：** 对应 $A_{uu}(\delta\boldsymbol{u},\boldsymbol{u})$，即弹性应变能项。使用弹性刚度矩阵 $\mathbf{K}_\varepsilon$（公式 (37)）代替质量矩阵 $\mathbf{K}_0$。注意 $\mathbf{K}_\varepsilon$ 包含 $x$-$y$ 耦合（通过 $\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}$ 中的剪切项），因此 $\mathbf{K}_{uu}$ 是**完整的 $2\times 2$ 块矩阵**（非块对角），而 $\mathbf{K}_{vv}$、$\mathbf{K}_{vu}$、$\mathbf{K}_{uv}$ 是块对角的（各方向独立）。

**整体算法流程小结：**

$$\underbrace{\text{2D空间VEM} \Rightarrow \mathbf{K}_0,\mathbf{K}_\varepsilon}_{\text{一次计算}} \quad \xrightarrow{\otimes\; \mathbf{L},\mathbf{Q},\mathbf{R},\mathbf{W}} \quad \underbrace{\mathbf{K}_{vv},\mathbf{K}_{vu},\mathbf{K}_{uv},\mathbf{K}_{uu}}_{\text{Kronecker 积组合}} \quad \Rightarrow \quad \underbrace{\mathbf{K}}_{\text{整体刚度矩阵}}$$

---

## 4. 有限元方法对比

本节给出用 **标准 FEM** 实现相同空间-时间框架的公式，验证公式 (51)-(54) 的通用性。

#### 公式 (55)：FEM 质量矩阵

$$\mathbf{K}^0 = \int_{\kappa_x}[\phi_\mathrm{FEM}][\phi_\mathrm{FEM}]^T\,\mathrm{d}\Omega \tag{55}$$

**说明：** 这是标准 FEM 的**一致质量矩阵**。$\phi_\mathrm{FEM}$ 是有限元形函数（如双线性四边形 $Q_1$ 形函数）。对比 VEM 的 $\mathbf{K}_0$（公式 (45)），FEM 版本直接用显式形函数积分，无需投影算子。

---

#### 公式 (56)：FEM 弹性刚度矩阵

$$\mathbf{K}^\varepsilon = \int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega \tag{56}$$

**说明：** 这是标准 FEM 的**一致刚度矩阵**（基于 $\mathbf{B}$-矩阵方法）。$\mathbf{B}$ 是应变-位移矩阵（见公式 (57)）。

---

#### 公式 (57)：FEM $\mathbf{B}$-矩阵

$$\mathbf{B} := \begin{bmatrix}\partial\phi_\mathrm{FEM}^T/\partial x & \mathbf{0}^T\\\mathbf{0}^T & \partial\phi_\mathrm{FEM}^T/\partial y\\\partial\phi_\mathrm{FEM}^T/\partial y & \partial\phi_\mathrm{FEM}^T/\partial x\end{bmatrix} \tag{57}$$

**说明：** $\mathbf{B}$-矩阵将节点位移 DOF 映射到 Voigt 应变向量：$\tilde{\boldsymbol{\varepsilon}} = \mathbf{B}\hat{\boldsymbol{u}}$。

> **数学事实 26（$\mathbf{B}$-矩阵与 Voigt 应变）：** 利用插值 $\boldsymbol{u} = \phi_\mathrm{FEM}^T\hat{\boldsymbol{u}}$，代入应变公式 (8)/(34)：
> $$\varepsilon_{xx}=\partial u_x/\partial x = (\partial\phi_\mathrm{FEM}^T/\partial x)\hat{u}_x, \quad \varepsilon_{yy}=(\partial\phi_\mathrm{FEM}^T/\partial y)\hat{u}_y$$
> $$\gamma_{xy}=\partial u_x/\partial y+\partial u_y/\partial x = (\partial\phi_\mathrm{FEM}^T/\partial y)\hat{u}_x+(\partial\phi_\mathrm{FEM}^T/\partial x)\hat{u}_y$$
> 整理成矩阵形式即得公式 (57)。

将公式 (55)-(57) 代入公式 (51)-(54)，即可得到**与 VEM 完全相同框架下的 FEM 实现**，代码结构几乎相同，差异仅在空间矩阵 $\mathbf{K}_0$、$\mathbf{K}_\varepsilon$ 的计算方式上（VEM 用投影，FEM 用 $\mathbf{B}$-矩阵）。

---

## 5. 数值算例概述

论文通过四个典型算例验证方法的精度和稳定性（无新公式推导，主要为参数研究和对比）：

### 5.1 悬臂梁横向振动

- **目的：** 研究稳定化参数 $\theta$ 对结果的影响。  
- **结论：** $\theta\in[0,0.8]$ 时影响较小，推荐 $\theta=0.01$（兼顾稳定性和精度）。空间-时间格式无相位误差（相比 Newmark 方法的相位漂移）。

### 5.2 Cook 膜片问题

- **目的：** 验证收敛性（网格细化研究）。  
- **结论：** 随着网格加密，空间-时间 VEM 收敛到参考解，精度与 FEM 相当。

### 5.3 含中心斜裂纹矩形板

- **目的：** 展示 VEM 处理任意多边形网格（裂纹界面处节点数任意）的灵活性。  
- **结论：** 对不同裂纹倾角，空间-时间 VEM 均能给出与 Newmark 方法一致的结果。

### 5.4 体力作用下的任意几何体

- **目的：** 展示 VEM 在任意几何形状（动物形状）域上的适用性。  
- **结论：** 复杂几何无需特殊处理，直接使用多边形网格。

---

## 6. 结论

本文建立了求解二维弹性动力学问题的**空间-时间虚元方法**，完整的公式体系可总结如下：

| 阶段 | 核心公式 | 数学工具 |
|:---|:---|:---|
| 变分出发点 | 公式 (1)-(4)：Hamilton 原理 | 分析力学、Fubini 定理 |
| 混合弱形式 | 公式 (10)-(14)：两场弱形式 | 变分法、时间分部积分 |
| 稳定化格式 | 公式 (18)-(19)：时间迎风 | Petrov-Galerkin 方法 |
| VEM 投影 | 公式 (20)-(25)：Ritz/$L^2$ 投影 | Hilbert 空间投影理论 |
| 空间矩阵 | 公式 (27)-(37)：$\mathbf{K}_0$、$\mathbf{K}_\varepsilon$ | Voigt 记号、稳定化 VEM |
| 时-空组合 | 公式 (38)-(54)：Kronecker 积 | 张量积、Gauss 积分 |
| FEM 对比 | 公式 (55)-(57)：$\mathbf{B}$-矩阵 FEM | 标准有限元理论 |

方法的核心优势：**"结构化"张量积方法**将三维时空问题分解为二维空间计算（VEM）加一维时间计算（FEM），通过 Kronecker 积高效组合，避免了在时间方向引入额外自由度，实现了高精度、无相位误差的弹性动力学求解。

---

*本文档按照论文原文的章节顺序撰写，公式编号与原文一一对应。*
