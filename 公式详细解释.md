# 《时空虚元法弹性动力学：理论、应用与代码开发》公式逐一详细解释

> 本文档对论文 *Space-time virtual element method for elastodynamics: Theory, applications, and code development*（文献翻译.md）中出现的全部公式（编号1至57）进行逐一解释，给出必要的推导过程，并阐明公式之间的内在联系。所有公式按论文分节组织。

---

## 第2节：变分公式（Variational Formulation）

### 2.1 基于Hamilton原理的变分公式

---

#### 公式 (1)：时空柱体的定义

$$Q \coloneqq \Omega \times I$$

**符号说明：**
- $\Omega \subset \mathbb{R}^n$（$n=1,2,3$）为空间域（有界 Lipschitz 区域）
- $I = (0, T)$ 为时间区间，$t \in I$
- $Q$ 为**时空柱体**（space-time cylinder），即空间域与时间区间的笛卡尔积

**物理含义：** 将时间视作额外的"空间维度"，从而把求解动力学问题转化为在高维时空域 $Q$ 上求解边值问题。这是整篇论文的核心思想起点。

---

#### 公式 (2)：Hamilton 作用量的定义

$$H \coloneqq \int_{\Omega}\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}\boldsymbol{\rho}\cdot \mathrm{d}\boldsymbol{u}\,\mathrm{d}V = \int_{\Omega}\int_{0}^{T}\rho\,\nabla_{t}\boldsymbol{u}\cdot\nabla_{t}\boldsymbol{u}\,\mathrm{d}t\,\mathrm{d}V = \int_{I}\int_{\Omega}\rho\|\nabla_{t}\boldsymbol{u}\|^{2}\,\mathrm{d}V\,\mathrm{d}t = \int_{I}2\mathcal{K}\,\mathrm{d}t$$

**推导过程：**

Hamilton 作用量的原始定义是将**动量密度向量** $\boldsymbol{\rho} \coloneqq \rho\,\nabla_t\boldsymbol{u}$ 沿位移路径 $[\boldsymbol{u}_0, \boldsymbol{u}_T]$ 积分，其中 $\nabla_t = \frac{\mathrm{d}}{\mathrm{d}t}$ 为时间导数算符，$\rho$ 为质量密度：

$$H = \int_{\Omega}\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}\boldsymbol{\rho}\cdot \mathrm{d}\boldsymbol{u}\,\mathrm{d}V$$

将 $\boldsymbol{\rho} = \rho\,\nabla_t\boldsymbol{u}$ 代入，并将路径积分写成对时间的积分（$\mathrm{d}\boldsymbol{u} = \nabla_t\boldsymbol{u}\,\mathrm{d}t$）：

$$H = \int_{\Omega}\int_{0}^{T}\rho\,\nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}t\,\mathrm{d}V$$

调换积分次序，并利用范数记号 $\|\nabla_t\boldsymbol{u}\|^2 = \nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u}$：

$$H = \int_{I}\int_{\Omega}\rho\|\nabla_t\boldsymbol{u}\|^{2}\,\mathrm{d}V\,\mathrm{d}t$$

注意到动能为 $\mathcal{K} = \int_{\Omega}\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V$（见公式(3)），所以：

$$H = \int_{I}2\mathcal{K}\,\mathrm{d}t$$

**与公式(3)的联系：** 公式(2)表明 Hamilton 作用量等于动能对时间积分的两倍，是公式(3)动能定义的直接推论。

---

#### 公式 (3)：动能

$$\mathcal{K} = \int_{\Omega}\frac{1}{2}\rho\|\nabla_{t}\boldsymbol{u}\|^{2}\,\mathrm{d}V$$

**物理含义：** 这是经典连续介质力学中的标准动能定义。$\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2$ 为单位体积的动能密度，$\nabla_t\boldsymbol{u} = \dot{\boldsymbol{u}}$ 即速度场。

**与公式(2)的联系：** 由公式(2)可得 $H = \int_I 2\mathcal{K}\,\mathrm{d}t$，公式(3)是此关系的桥梁。

---

#### 公式 (4)：能量守恒（热力学第一定律）

$$\int_{I}\mathcal{K}\,\mathrm{d}t + \int_{I}\mathcal{G}\,\mathrm{d}t = 0$$

**推导说明：** 热力学第一定律要求动能的时间积分与总势能的时间积分之和为零（保守系统）。其中 $\mathcal{G}$ 为总势能（见公式(5)）。

**物理意义：** 在保守弹性动力学系统中，动能与势能之和守恒，两者随时间的积分之和为零（对应变分原理中的驻值条件）。

**与公式(9)的联系：** 由公式(4)可推出 Hamilton 作用量的正式表达式 $H = \int_I (\mathcal{K} - \mathcal{G})\,\mathrm{d}t$（公式(9)），这正是 Hamilton 原理的经典形式（作用量 $=$ 动能 $-$ 势能对时间的积分）。

---

#### 公式 (5)：总势能的定义

$$\mathcal{G} \coloneqq \int_{\Omega}\boldsymbol{\psi}\,\mathrm{d}V - \int_{\Omega}\bar{\boldsymbol{f}}\cdot\boldsymbol{u}\,\mathrm{d}V - \int_{\Gamma_{\sigma}}\bar{\boldsymbol{t}}\cdot\boldsymbol{u}\,\mathrm{d}\Gamma$$

**符号说明：**
- $\boldsymbol{\psi}$ 为弹性应变能密度（自由能密度，见公式(6)）
- $\bar{\boldsymbol{f}}$ 为体积力（volume force）
- $\bar{\boldsymbol{t}}$ 为 Neumann 边界 $\Gamma_{\sigma}$ 上的外加面力（traction）
- 负号表示外力做的功降低势能

**物理含义：** 总势能等于应变能减去外力势能（体力与面力的功）。这与结构力学中总势能的经典定义一致：$\Pi = U - W$（应变能 $-$ 外力功）。

**与公式(6)、(8)的联系：** $\boldsymbol{\psi}$ 由公式(6)给出，而应变 $\epsilon$ 由公式(8)给出，三者共同构成势能的完整表达。

---

#### 公式 (6)：弹性自由能密度

$$\boldsymbol{\psi} \coloneqq \frac{\lambda}{2}\left(\mathrm{tr}(\epsilon)\right)^{2} + \mu\,\epsilon:\epsilon = \frac{1}{2}\epsilon:\mathbb{D}:\epsilon$$

**推导说明：** 对于各向同性线弹性材料，应变能密度（Helmholtz 自由能）由 Lamé 参数 $\lambda$ 和 $\mu$ 表征：

**第一项** $\frac{\lambda}{2}(\mathrm{tr}(\epsilon))^2$：与体积膨胀（静水压力）有关，$\mathrm{tr}(\epsilon) = \epsilon_{11}+\epsilon_{22}+\epsilon_{33}$ 为体积应变。

**第二项** $\mu\,\epsilon:\epsilon$：与剪切变形有关，$\epsilon:\epsilon = \epsilon_{ij}\epsilon_{ij}$（张量双缩并）。

利用弹性张量 $\mathbb{D}$（公式(7)），可以将两项统一写成 $\frac{1}{2}\epsilon:\mathbb{D}:\epsilon$，即：

$$\epsilon:\mathbb{D}:\epsilon = \mathbb{D}_{ijkl}\epsilon_{ij}\epsilon_{kl} = \lambda(\mathrm{tr}(\epsilon))^2 + 2\mu\,\epsilon:\epsilon$$

所以 $\frac{1}{2}\epsilon:\mathbb{D}:\epsilon = \frac{\lambda}{2}(\mathrm{tr}(\epsilon))^2 + \mu\,\epsilon:\epsilon$，与第一个等式吻合。

**与公式(7)的联系：** 公式(6)的紧凑形式 $\frac{1}{2}\epsilon:\mathbb{D}:\epsilon$ 需要公式(7)中弹性张量 $\mathbb{D}$ 的分量表达式支撑。

---

#### 公式 (7)：各向同性弹性张量分量

$$\mathbb{D}_{ijkl} = \lambda\,\delta_{ij}\delta_{kl} + \mu\left(\delta_{ik}\delta_{jl} + \delta_{il}\delta_{jk}\right)$$

**符号说明：**
- $\lambda, \mu$ 为 Lamé 常数，与工程常用参数的换算关系为：$\lambda = \frac{E\nu}{(1+\nu)(1-2\nu)}$，$\mu = \frac{E}{2(1+\nu)}$，其中 $E$ 为杨氏模量，$\nu$ 为泊松比
- $\delta_{ij}$ 为 Kronecker 符号：$\delta_{ij} = 1$（$i=j$），$\delta_{ij} = 0$（$i\neq j$）

**推导说明：** 各向同性弹性张量满足材料对称性要求（对所有旋转不变）。其最一般形式只含两个独立参数 $\lambda$ 和 $\mu$。验证公式(6)：

$$\mathbb{D}_{ijkl}\epsilon_{ij}\epsilon_{kl} = \lambda\delta_{ij}\epsilon_{ij}\delta_{kl}\epsilon_{kl} + \mu(\delta_{ik}\epsilon_{ij}\delta_{jl}\epsilon_{kl} + \delta_{il}\epsilon_{ij}\delta_{jk}\epsilon_{kl})$$
$$= \lambda(\mathrm{tr}\,\epsilon)^2 + \mu(\epsilon_{ij}\epsilon_{ij} + \epsilon_{ij}\epsilon_{ji}) = \lambda(\mathrm{tr}\,\epsilon)^2 + 2\mu\,\epsilon:\epsilon$$

（利用了 $\epsilon$ 的对称性 $\epsilon_{ij}=\epsilon_{ji}$）。这验证了 $\frac{1}{2}\epsilon:\mathbb{D}:\epsilon = \frac{\lambda}{2}(\mathrm{tr}\,\epsilon)^2 + \mu\,\epsilon:\epsilon$（公式(6)）。

---

#### 公式 (8)：小应变张量（对称化梯度）

$$\epsilon = \frac{1}{2}\left(\nabla\boldsymbol{u} + \nabla\boldsymbol{u}^T\right) =: \nabla^{\mathrm{sym}}\boldsymbol{u} = \epsilon(\boldsymbol{u})$$

**推导说明：** 在小变形假设下，应变张量定义为位移梯度的对称部分。位移梯度 $\nabla\boldsymbol{u}$ 可分解为对称部分（应变）和反对称部分（刚体转动）：

$$\nabla\boldsymbol{u} = \underbrace{\frac{1}{2}(\nabla\boldsymbol{u}+\nabla\boldsymbol{u}^T)}_{\epsilon\text{（应变）}} + \underbrace{\frac{1}{2}(\nabla\boldsymbol{u}-\nabla\boldsymbol{u}^T)}_{\boldsymbol{\omega}\text{（旋转）}}$$

分量形式为 $\epsilon_{ij} = \frac{1}{2}(u_{i,j} + u_{j,i})$，这是经典的 Cauchy 小应变张量。

**与公式(5)、(6)、(7)的联系：** $\epsilon$ 是构建应变能密度 $\boldsymbol{\psi}$（公式(6)）、弹性张量双缩并（公式(7)）以及后续变分方程（公式(13)、(14)）的基本量。

---

#### 公式 (9)：Hamilton 作用量（经典形式）

$$H = \int_{I}(\mathcal{K} - \mathcal{G})\,\mathrm{d}t$$

**推导说明：** 由公式(4)（能量守恒）：

$$\int_I\mathcal{K}\,\mathrm{d}t + \int_I\mathcal{G}\,\mathrm{d}t = 0$$

可得 $\int_I\mathcal{K}\,\mathrm{d}t = -\int_I\mathcal{G}\,\mathrm{d}t$。

Hamilton 作用量的经典定义为 $H = \int_I L\,\mathrm{d}t$，其中拉格朗日量 $L = \mathcal{K} - \mathcal{G}$（动能 $-$ 势能）。这与公式(2)和公式(4)的结合完全一致。

**物理意义：** Hamilton 原理要求作用量 $H$ 取极值（驻值），即 $\delta H = 0$，从而导出控制方程（弹性动力学方程）。这是公式(11)的出发点。

---

#### 公式 (10)：速度独立变量引入后的动能（混合公式）

$$\mathcal{K} = \int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2\,\mathrm{d}\Omega$$

**推导说明（混合公式的由来）：** 将速度 $\boldsymbol{v}$ 作为独立变量引入（混合变分公式），原动能 $\mathcal{K} = \int_\Omega \frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2\,\mathrm{d}V$ 可通过"补全平方"重写：

$$\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2 = \rho\,\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2 + \frac{1}{2}\rho\|\nabla_t\boldsymbol{u} - \boldsymbol{v}\|^2$$

当 $\boldsymbol{v} \equiv \nabla_t\boldsymbol{u}$（即速度等于位移的时间导数）时，最后一项消失，公式(10)退化为公式(3)中的原始动能。

**为何引入混合公式？** 将 $\boldsymbol{v}$ 作为独立变量后，Hamilton 作用量对 $\boldsymbol{v}$ 的变分自然给出**约束方程** $\boldsymbol{v} = \nabla_t\boldsymbol{u}$（公式(12)-(13)的 $\delta_v H = 0$），而对 $\boldsymbol{u}$ 的变分给出**动量方程**（公式(12)-(13)的 $\delta_u H = 0$）。这种混合公式便于数值离散（见公式(14)及后续双线性形式）。

---

#### 公式 (11)：Hamilton 驻值原理（变分形式）

$$H = H\left[\boldsymbol{v},\boldsymbol{u},\nabla^{\mathrm{sym}}\boldsymbol{u},\nabla_t\boldsymbol{u}\right] \to \mathrm{stat} \;\Leftrightarrow\; \delta H = \delta_{\boldsymbol{v}}H + \delta_{\boldsymbol{u}}H = 0 \quad \forall\,\delta\boldsymbol{v},\,\delta\boldsymbol{u}$$

**推导说明：** Hamilton 原理的驻值条件要求作用量 $H$ 对所有容许变分 $\delta\boldsymbol{v}$（速度场的变分）和 $\delta\boldsymbol{u}$（位移场的变分）的一阶变分为零。

由于 $H$ 是 $\boldsymbol{v}$、$\boldsymbol{u}$、$\nabla^{\mathrm{sym}}\boldsymbol{u}$、$\nabla_t\boldsymbol{u}$ 的泛函，全变分为：

$$\delta H = \frac{\partial H}{\partial\boldsymbol{v}}\cdot\delta\boldsymbol{v} + \frac{\partial H}{\partial\boldsymbol{u}}\cdot\delta\boldsymbol{u} + \frac{\partial H}{\partial\nabla^{\mathrm{sym}}\boldsymbol{u}}:\delta\nabla^{\mathrm{sym}}\boldsymbol{u} + \frac{\partial H}{\partial\nabla_t\boldsymbol{u}}\cdot\delta\nabla_t\boldsymbol{u} = 0$$

由 $\delta\boldsymbol{v}$ 和 $\delta\boldsymbol{u}$ 的独立性，可分别令对应项为零（见公式(12)）。

---

#### 公式 (12)：驻值条件的分解

$$\left\{\begin{array}{ll}\delta_{\boldsymbol{v}}H = 0 & \forall\,\delta\boldsymbol{v} \\ \delta_{\boldsymbol{u}}H = 0 & \forall\,\delta\boldsymbol{u}\end{array}\right.$$

**推导说明：** 由于 $\delta\boldsymbol{v}$ 和 $\delta\boldsymbol{u}$ 相互独立（混合公式），公式(11)中的 $\delta H = 0$ 可以分解为两个独立的方程。第一个方程（$\delta_{\boldsymbol{v}}H = 0$）给出速度与位移时间导数之间的关系；第二个方程（$\delta_{\boldsymbol{u}}H = 0$）给出弹性动力学方程的弱形式。

**与公式(13)的联系：** 公式(12)的两个条件在公式(13)中给出了具体的积分形式。

---

#### 公式 (13)：变分方程的具体形式（分部积分前）

$$\left\{\begin{array}{ll}
\delta_{\boldsymbol{v}}H = \displaystyle\int_{I}\int_{\Omega}\rho(\nabla_t\boldsymbol{u} - \boldsymbol{v})\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\[8pt]
\delta_{\boldsymbol{u}}H = \displaystyle\int_{I}\int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t - \int_{I}\!\left(\int_{\Omega}\!\left(\boldsymbol{\sigma}:\epsilon(\delta\boldsymbol{u}) - \bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\right)\mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\!\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{u}
\end{array}\right.$$

**推导过程（第一个方程）：** 对公式(10)中的 $\mathcal{K}$ 关于 $\boldsymbol{v}$ 取变分：

$$\delta_{\boldsymbol{v}}\mathcal{K} = \int_\Omega\rho(\nabla_t\boldsymbol{u} - \boldsymbol{v})\cdot\delta\boldsymbol{v}\,\mathrm{d}\Omega$$

对时间积分后令其为零即得第一式。由任意性 $\delta\boldsymbol{v}$ 可得 $\boldsymbol{v} = \nabla_t\boldsymbol{u}$（速度约束）。

**推导过程（第二个方程）：** 对 $H = \int_I(\mathcal{K}-\mathcal{G})\,\mathrm{d}t$ 关于 $\boldsymbol{u}$ 取变分。

**对 $\mathcal{K}$ 的贡献**（使用公式(10)）：
$$\delta_{\boldsymbol{u}}\mathcal{K} = \int_\Omega\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}\Omega$$

**对 $-\mathcal{G}$ 的贡献**（使用公式(5),(6)）：
$$-\delta_{\boldsymbol{u}}\mathcal{G} = -\int_\Omega\frac{\partial\boldsymbol{\psi}}{\partial\epsilon}:\delta\epsilon\,\mathrm{d}V + \int_\Omega\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}V + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma$$

由于 $\boldsymbol{\sigma} = \frac{\partial\boldsymbol{\psi}}{\partial\epsilon} = \mathbb{D}:\epsilon$，合并得第二式。

---

#### 公式 (14)：弱形式（分部积分后）

$$\left\{\begin{array}{ll}
\displaystyle\int_{I}\int_{\Omega}-\rho\,\boldsymbol{v}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t + \int_{I}\int_{\Omega}\rho\,\nabla_t\boldsymbol{u}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\[8pt]
\displaystyle\int_{I}\int_{\Omega}\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t + \int_{I}\int_{\Omega}\boldsymbol{\sigma}:\delta\nabla^{\mathrm{sym}}\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t = \int_{I}\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}V + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\!\mathrm{d}t
\end{array}\right.$$

**推导过程（时间分部积分）：** 对公式(13)第二式中的第一项 $\int_I\int_\Omega\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t$ 进行时间分部积分：

$$\int_0^T\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}t = \left[\rho\,\boldsymbol{v}\cdot\delta\boldsymbol{u}\right]_0^T - \int_0^T\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}t$$

由于测试函数 $\delta\boldsymbol{u}$ 满足 $\delta\boldsymbol{u}|_0 = \delta\boldsymbol{u}|_T \equiv \mathbf{0}$（端点条件），边界项消失，得到：

$$\int_I\int_\Omega\rho\,\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t = -\int_I\int_\Omega\rho\,\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t$$

将负号移到左边即得公式(14)第二式的第一项。同样，对公式(13)第一式进行类似处理可得第一式。

**物理解读：** 公式(14)是弹性动力学的时空弱形式（mixed weak form），两个方程分别对应：
- 方程1：速度与位移时间导数的关系（$\boldsymbol{v} = \nabla_t\boldsymbol{u}$ 的弱形式）
- 方程2：虚功原理（内力虚功 $=$ 外力虚功），等价于动量守恒方程 $\rho\ddot{\boldsymbol{u}} = \mathrm{div}\,\boldsymbol{\sigma} + \bar{\boldsymbol{f}}$ 的弱形式

---

### 2.2 时空双线性形式与时间迎风稳定化

---

#### 公式 (15)：双线性形式的矩阵结构

$$A(\boldsymbol{v},\boldsymbol{u},\delta\boldsymbol{v},\delta\boldsymbol{u}) = \begin{pmatrix}A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v}) + A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u})\\ A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v}) + A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u})\end{pmatrix} = \begin{pmatrix}\mathbf{0}\\ l(\delta\boldsymbol{u})\end{pmatrix}$$

**推导说明：** 将公式(14)的双场弱形式改写成双线性形式的矩阵结构。将两个方程的左端项（双线性形式）和右端项（线性型）整理为块向量形式：

- 第一行对应公式(14)第一式（$\delta\boldsymbol{v}$ 方程），右端为 $\mathbf{0}$
- 第二行对应公式(14)第二式（$\delta\boldsymbol{u}$ 方程），右端为外力线性型 $l(\delta\boldsymbol{u})$

各双线性型 $A^{\mathrm{trv}}$ 的具体定义见公式(16)。

---

#### 公式 (16)：四个双线性型的定义

$$\begin{array}{l}
A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v}) = \displaystyle\int_{0}^{T}\int_{\Omega}-\rho\,\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t \\[6pt]
A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u}) = \displaystyle\int_{0}^{T}\int_{\Omega}\rho\,\delta\boldsymbol{v}\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t \\[6pt]
A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v}) = \displaystyle\int_{0}^{T}\int_{\Omega}\rho\,\delta\boldsymbol{u}\cdot\nabla_t\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t \\[6pt]
A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u}) = \displaystyle\int_{0}^{T}\int_{\Omega}\epsilon(\delta\boldsymbol{u}):\mathbb{D}:\epsilon(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t
\end{array}$$

**各项物理含义：**

- $A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v})$：与质量矩阵对应，反映速度场的"质量"贡献（负号来自分部积分）
- $A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u})$：耦合项，联系速度测试函数与位移时间导数（约束 $\boldsymbol{v}=\nabla_t\boldsymbol{u}$ 的来源）
- $A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v})$：惯性项，联系位移测试函数与速度时间导数（惯性力的来源）
- $A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u})$：刚度项，即弹性应变能的双线性型

**与公式(14)的联系：** 这四个双线性型直接来自公式(14)的积分项，对号入座地拆分成单场双线性型。

---

#### 公式 (17)：外力线性型

$$l(\delta\boldsymbol{u}) = \int_{0}^{T}\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\!\mathrm{d}t$$

**物理含义：** 这是外力（体力 $\bar{\boldsymbol{f}}$ 和面力 $\bar{\boldsymbol{t}}$）对虚位移 $\delta\boldsymbol{u}$ 所做的虚功，即外力虚功原理的时空积分版本。

**与公式(19)的联系：** 引入迎风稳定化后，测试函数 $\delta\boldsymbol{u}$ 被替换为 $\delta\boldsymbol{u} + \theta h\nabla_t(\delta\boldsymbol{u})$，得到稳定化的线性型公式(19)。

---

#### 公式 (18)：时间迎风稳定化后的双线性型

$$\begin{aligned}
A_{\boldsymbol{v}\boldsymbol{v}}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_{0}^{T}\int_{\Omega}-\rho(\delta\boldsymbol{v}+\theta h\nabla_t(\delta\boldsymbol{v}))\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t \\
A_{\boldsymbol{v}\boldsymbol{u}}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_{0}^{T}\int_{\Omega}\rho(\delta\boldsymbol{v}+\theta h\nabla_t(\delta\boldsymbol{v}))\cdot\nabla_t\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t \\
A_{\boldsymbol{u}\boldsymbol{v}}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_{0}^{T}\int_{\Omega}\rho(\delta\boldsymbol{u}+\theta h\nabla_t(\delta\boldsymbol{u}))\cdot\nabla_t\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t \\
A_{\boldsymbol{u}\boldsymbol{u}}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_{0}^{T}\int_{\Omega}\epsilon(\delta\boldsymbol{u}+\theta h\nabla_t(\delta\boldsymbol{u})):\mathbb{D}:\epsilon(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t
\end{aligned}$$

**迎风稳定化原理：** 时间迎风稳定化（time-upwind stabilization，参考文献[36]）的核心思想来源于对流扩散方程的迎风差分格式。将测试函数 $\delta\boldsymbol{w}$ 替换为：

$$\delta\boldsymbol{w} \;\longrightarrow\; \delta\boldsymbol{w} + \theta h\nabla_t(\delta\boldsymbol{w})$$

其中 $\theta > 0$ 为稳定化参数，$h$ 为时间方向的单元尺寸（时间步长）。

**稳定化机制：** 附加项 $\theta h\nabla_t(\delta\boldsymbol{w})$ 引入了与时间导数成正比的"人工扩散"，抑制了数值振荡。这类似于 Streamline Upwind Petrov-Galerkin（SUPG）方法：通过修改测试空间而非试探空间来增强稳定性，保证了 Galerkin 正交性的同时提升了稳定性。

**参数 $\theta$ 的作用：** 数值算例（第5节）表明，$\theta$ 越大稳定性越强但引入的人工阻尼越多；$\theta$ 越小结果精度越高但可能不稳定。本文选取 $\theta = 0.01$。

**与公式(16)的联系：** 公式(18)是对公式(16)的扩展，通过替换测试函数增加稳定项。当 $\theta = 0$ 时，公式(18)退化为公式(16)。

---

#### 公式 (19)：稳定化后的外力线性型

$$l(\delta\boldsymbol{u}) = \int_{0}^{T}\!\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot(\delta\boldsymbol{u}+\theta h\nabla_t(\delta\boldsymbol{u}))\,\mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot(\delta\boldsymbol{u}+\theta h\nabla_t(\delta\boldsymbol{u}))\,\mathrm{d}\Gamma\right)\!\mathrm{d}t$$

**推导说明：** 与双线性型公式(18)保持一致，对公式(17)中的测试函数 $\delta\boldsymbol{u}$ 同样进行迎风替换 $\delta\boldsymbol{u} \to \delta\boldsymbol{u} + \theta h\nabla_t(\delta\boldsymbol{u})$，保证整个变分方程稳定化处理的一致性。

---

## 第3节：时空虚元法（VEM）

### 3.2 投影算符

---

#### 公式 (20)：$H^1$ 椭圆投影算符的定义（Ritz 投影）

$$a_E\!\left(w - \Pi_{\nabla}^{k,E}w,\, q_k\right) = 0 \quad \forall\,q_k \in P_k(E)$$

**符号说明：**
- $\Pi_{\nabla}^{k,E}: H^1(E) \to P_k(E)$ 为 $k$ 阶 $H^1$（椭圆/Ritz）投影算符
- $k$ 为 VEM 的阶数（本文取 $k=1$）
- $a_E(p,q) = \int_E\nabla p\cdot\nabla q\,\mathrm{d}\Omega$ 为局部双线性型
- $P_k(E)$ 为单元 $E$ 上不超过 $k$ 次的多项式空间

**物理含义（投影的意义）：** 公式(20)要求 $w - \Pi_{\nabla}^{k,E}w$（误差）在 $H^1$ 半范意义下与所有多项式 $q_k$ 正交：

$$\int_E\nabla(w - \Pi_{\nabla}^{k,E}w)\cdot\nabla q_k\,\mathrm{d}\Omega = 0 \quad \forall\,q_k \in P_k(E)$$

这意味着 $\Pi_{\nabla}^{k,E}w$ 是 $w$ 在 $H^1$ 半范下对多项式空间的**最佳逼近**（最小化 $|w - \Pi_{\nabla}^{k,E}w|_{H^1(E)}$）。

**VEM 的核心思想：** 在虚元法中，形函数 $\phi$ 在单元内部是隐式的（不显式计算），但其 $H^1$ 投影 $\Pi_{\nabla}^{k,E}\phi$ 是可计算的（只需边界信息和自由度）。这是 VEM 能避免显式计算形函数的关键。

---

#### 公式 (21)：投影算符的附加条件（消除常数不确定性）

$$\begin{cases}
\dfrac{1}{N_V^E}\displaystyle\sum_{i=1}^{N_V^E}\left(\Pi_{\nabla}^{k,E}w - w\right) = 0, & k=1 \\[8pt]
\dfrac{1}{|E|}\displaystyle\int_E\left(\Pi_{\nabla}^{k,E}w - w\right)\mathrm{d}\Omega = 0, & k > 1
\end{cases}$$

**推导说明：** 公式(20)中的 $H^1$ 半范投影条件 $a_E(w-\Pi_{\nabla}^{k,E}w, q_k) = 0$ 只约束了梯度，对常数项没有约束（因为 $\nabla c = \mathbf{0}$ 对任意常数 $c$）。因此需要附加一个"零均值"条件来唯一确定投影。

- 对 $k=1$：要求投影误差在所有顶点处的平均值为零（顶点平均条件）
- 对 $k>1$：要求投影误差在单元内的积分均值为零（$L^2$ 均值条件）

**与公式(24)的联系：** 将公式(21)与公式(20)联立，代入公式(24)中的自由度展开式，即可得到投影矩阵 $\mathbf{\Pi}_\nabla^{k,E}$ 的显式计算公式。

---

#### 公式 (22)：虚元空间的定义

$$\mathcal{V}_h^E := \left\{v \in H^1(E) \;:\; v|_{\partial E} \in \mathcal{B}(\partial E),\; \Delta v \in P_{k-2}(E)\right\}$$

**推导说明：** 虚元空间 $\mathcal{V}_h^E$ 由以下两个条件定义：

1. **边界条件** $v|_{\partial E} \in \mathcal{B}(\partial E)$：在单元边界上，$v$ 属于边界元素空间 $\mathcal{B}(\partial E)$（公式(23)），即在每条边上是 $k$ 次多项式
2. **内部方程** $\Delta v \in P_{k-2}(E)$：$v$ 的 Laplacian 是低次多项式（控制内部自由度）

**VEM 的"虚"之含义：** 空间 $\mathcal{V}_h^E$ 中的函数在单元内部是隐式的（满足低次多项式 Laplacian 方程），但不需要显式写出。这与 FEM 完全不同——FEM 需要显式的形函数。VEM 只需利用公式(20)定义的投影算符就能构造所有需要的矩阵。

对 $k=1$：$\Delta v \in P_{-1}(E) = \{0\}$，即 $v$ 在单元内部是**调和函数**（满足 Laplace 方程 $\Delta v = 0$）。

---

#### 公式 (23)：边界元素空间的定义

$$\mathcal{B}(\partial E) := \left\{v \in C^0(\partial E) \;:\; v_F \in P_k(F),\; \forall\,F \in \partial E\right\}$$

**解释：** $\mathcal{B}(\partial E)$ 是单元边界 $\partial E$ 上的连续函数空间，要求在每条边 $F$ 上限制为 $k$ 次多项式。对 $k=1$，这意味着在每条边上是线性插值，整个边界是分段线性的（$C^0$ 连续）。

**自由度：** 对 $k=1$，$\mathcal{B}(\partial E)$ 的自由度即为 $E$ 的所有顶点处的函数值（共 $N_V^E$ 个）。

---

#### 公式 (24)：自由度展开与投影的矩阵表示

$$v_h = \boldsymbol{\phi}^T\hat{v}_h, \qquad \Pi_{\nabla}^{k,E}v_h = \boldsymbol{m}_k^T\Pi_{\nabla}^{k,E}\hat{v}_h = \boldsymbol{\phi}^T\mathbf{\Pi}_{\nabla}^{k,E}\hat{v}_h$$

**符号说明：**
- $\boldsymbol{\phi}$：VEM 空间 $\mathcal{V}_h^E$ 的基函数向量（隐式，不需要显式计算）
- $\boldsymbol{m}_k$：多项式空间 $P_k$ 的基函数向量（如 $[1, x, y]^T$ 对 $k=1$）
- $\hat{v}_h$：自由度向量（顶点值）
- $\mathbf{\Pi}_{\nabla}^{k,E}$：投影算符的矩阵表示

**推导说明：** 将公式(20)和(21)代入，利用 VEM 空间基函数的定义，可以建立投影矩阵 $\mathbf{\Pi}_{\nabla}^{k,E}$ 的计算公式。关键步骤是：由于 $\Pi_{\nabla}^{k,E}v_h \in P_k$，可以用多项式基函数 $\boldsymbol{m}_k$ 展开；而公式(20)的正交条件在离散化后变为线性系统，从而求解 $\mathbf{\Pi}_{\nabla}^{k,E}$（Ritz 投影矩阵）。

---

#### 公式 (25)：$L^2$ 正交投影算符的定义

$$\left(v - \Pi_{k,E}^0 v,\, q_k\right)_E = 0 \quad \forall\,q_k \in P_k(E)$$

**推导说明：** $L^2$ 正交投影 $\Pi_{k,E}^0: L^2(E) \to P_k(E)$ 要求投影误差在 $L^2$ 意义下与所有多项式正交：

$$\int_E\left(v - \Pi_{k,E}^0 v\right)q_k\,\mathrm{d}\Omega = 0 \quad \forall\,q_k \in P_k(E)$$

这等价于 $\Pi_{k,E}^0 v$ 是 $v$ 在 $L^2$ 内积下对 $P_k$ 的最佳逼近（最小化 $\|v - \Pi_{k,E}^0 v\|_{L^2(E)}$）。

**与公式(20)的区别：** $\Pi_{\nabla}^{k,E}$（Ritz 投影）基于 $H^1$ 半范（梯度），$\Pi_{k,E}^0$（$L^2$ 投影）基于 $L^2$ 范数（函数值）。论文指出，对 $k \leq 2$ 有 $\Pi_{\nabla}^{k,E} = \Pi_{k,E}^0$，所以对本文 $k=1$ 的情形无需额外计算 $L^2$ 投影。

**为何需要 $L^2$ 投影？** 观察公式(18)的前三个双线性型，它们含有 $\boldsymbol{v}\cdot\boldsymbol{u}$ 或 $\boldsymbol{v}\cdot\nabla_t\boldsymbol{u}$ 类型的项，在离散化时需要计算 $\int_E\phi_i\phi_j\,\mathrm{d}\Omega$（质量矩阵），这需要 $L^2$ 投影而非 $H^1$ 投影。

---

### 3.3 空间域中的离散双线性形式

---

#### 公式 (26)（未显式给出）

论文在公式(25)和公式(27)之间隐含了 VEM 逼近的一致性和稳定性条件（文中称为"k-consistency"和"k-stability"），这是 VEM 双线性型的标准设计原则。设 $a^E_h$ 为真实双线性型 $a^E$ 的 VEM 近似，需满足：
- **一致性**：$a^E_h(q_k, v_h) = a^E(q_k, v_h)$ 对所有 $q_k \in P_k(E)$ 成立（投影是精确的）
- **稳定性**：存在正常数 $c_1, c_2$ 使得 $c_1 a^E(v_h,v_h) \leq a^E_h(v_h,v_h) \leq c_2 a^E(v_h,v_h)$

这两个性质是公式(27)中稳定化项设计的理论依据。

---

#### 公式 (27)：VEM 离散双线性型（含稳定化）

$$\begin{aligned}
a_h^E(p_h,q_h) &:= a^E(\Pi_{\nabla}^{k,E}p_h,\,\Pi_{\nabla}^{k,E}q_h) + S^E\!\left((\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{p}_h,\,(\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{q}_h\right) \\[4pt]
m_h^E(p_h,q_h) &:= m^E(\Pi_{k,E}^0 p_h,\,\Pi_{k,E}^0 q_h) + R^E\!\left((\mathbf{I}-\Pi_{k,E}^0)\hat{p}_h,\,(\mathbf{I}-\Pi_{k,E}^0)\hat{q}_h\right)
\end{aligned}$$

**结构分析：** 每个 VEM 双线性型由**两部分**组成：

1. **一致性项**（consistency part）：$a^E(\Pi_{\nabla}^{k,E}p_h, \Pi_{\nabla}^{k,E}q_h)$
   - 用投影后的多项式计算精确积分，保证多项式的精确性
   - 这是"可计算"的部分（因为投影是多项式）

2. **稳定化项**（stabilization part）：$S^E((\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{p}_h, (\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{q}_h)$
   - $(\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{p}_h$ 是未被投影"捕获"的自由度部分
   - 稳定化项防止零能模式（核空间非平凡的情况），保证 VEM 刚度矩阵的正定性
   - $S^E$ 是任意正定双线性型（通常取简单形式，如恒等算符），其具体选取见公式(30)-(31)

**类比理解：** 类似于 FEM 中的约化积分加稳定化（如 Hourglass 控制），VEM 通过"投影+稳定化"两步构造可计算但稳定的双线性型。

---

#### 公式 (28)：标量场的椭圆双线性型（矩阵显式计算）

$$\begin{aligned}
a_h^E(p,q) &= \int_{\mathcal{K}_x}\left(\nabla\Pi_{\nabla}^{k,E}p_h\right)\cdot\left(\nabla\Pi_{\nabla}^{k,E}q_h\right)\mathrm{d}\Omega + S^E((\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E})\hat{q}_h) \\
&= \hat{p}_h^T\!\left[(\mathbf{\Pi}_{\nabla}^{k,E*})^T\!\int_{\mathcal{K}_x}\nabla\boldsymbol{m}_k\nabla\boldsymbol{m}_k^T\mathrm{d}\Omega\;\mathbf{\Pi}_{\nabla}^{k,E*} + (\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E})\right]\hat{q}_h \\
&= \hat{p}_h^T(\mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla)\hat{q}_h = \hat{p}_h^T\mathbf{K}_\nabla\hat{q}_h
\end{aligned}$$

**逐步推导：**

**第一步**（利用公式(24)代入）：$\Pi_{\nabla}^{k,E}p_h = \boldsymbol{m}_k^T\mathbf{\Pi}_{\nabla}^{k,E*}\hat{p}_h$，其中 $\mathbf{\Pi}_{\nabla}^{k,E*}$ 是投影矩阵的"右作用"形式。于是：

$$\nabla\Pi_{\nabla}^{k,E}p_h = (\nabla\boldsymbol{m}_k^T)\mathbf{\Pi}_{\nabla}^{k,E*}\hat{p}_h$$

**第二步**（代入积分）：

$$\int_{\mathcal{K}_x}(\nabla\Pi_{\nabla}^{k,E}p_h)\cdot(\nabla\Pi_{\nabla}^{k,E}q_h)\,\mathrm{d}\Omega = \hat{p}_h^T(\mathbf{\Pi}_{\nabla}^{k,E*})^T\underbrace{\int_{\mathcal{K}_x}\nabla\boldsymbol{m}_k\nabla\boldsymbol{m}_k^T\,\mathrm{d}\Omega}_{=:\mathbf{G}}\mathbf{\Pi}_{\nabla}^{k,E*}\hat{q}_h$$

**第三步**（稳定化项取恒等形式）：$S^E((\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h) = \hat{p}_h^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_\nabla^{k,E})\hat{q}_h$

合并得到 $\mathbf{K}_\nabla = \mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla$（见公式(31)的定义）。

---

#### 公式 (29)：标量场的质量双线性型（矩阵显式计算）

$$\begin{aligned}
m_h^E(p,q) &= \int_{\mathcal{K}_x}\left(\Pi_{k,E}^0 p_h\right)\cdot\left(\Pi_{k,E}^0 q_h\right)\mathrm{d}\Omega + R^E((\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{p}_h,(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\hat{q}_h) \\
&= \hat{p}_h^T\!\left[(\mathbf{\Pi}_{k,E}^{0*})^T\!\int_{\mathcal{K}_x}\boldsymbol{m}_k\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_{k,E}^{0*} + |E|(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)^T(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)\right]\hat{q}_h \\
&= \hat{p}_h^T(\mathbf{K}_c^0 + \mathbf{K}_s^0)\hat{q}_h = \hat{p}_h^T\mathbf{K}_0\hat{q}_h
\end{aligned}$$

**与公式(28)的对比：** 推导结构完全类似，区别在于：
- 公式(28)用的是梯度投影 $\Pi_\nabla^{k,E}$，积分核为 $\nabla\boldsymbol{m}_k\nabla\boldsymbol{m}_k^T$（刚度矩阵型）
- 公式(29)用的是 $L^2$ 投影 $\Pi_{k,E}^0$，积分核为 $\boldsymbol{m}_k\boldsymbol{m}_k^T$（质量矩阵型）
- 稳定化项中多了 $|E|$ 因子（面积因子），保证稳定化项的量纲一致

**与公式(33)的联系：** 公式(29)给出了标量的 $\mathbf{K}_0$ 矩阵，公式(33)将其扩展为向量场（位移/速度）的质量矩阵 $\mathbb{K}_0$。

---

#### 公式 (30)：$L^2$ 投影矩阵的定义

$$\begin{aligned}
\mathbf{K}_c^0 &:= (\mathbf{\Pi}_{k,E}^{0*})^T\int_{\mathcal{K}_x}\boldsymbol{m}_k\boldsymbol{m}_k^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_{k,E}^{0*} \\
\mathbf{K}_s^0 &:= |E|(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)^T(\mathbf{I}-\mathbf{\Pi}_{k,E}^0) \\
\mathbf{K}_0 &:= \mathbf{K}_c^0 + \mathbf{K}_s^0
\end{aligned}$$

**物理含义：**
- $\mathbf{K}_c^0$（一致性质量矩阵）：通过多项式基函数的精确积分构造，保证对多项式精确
- $\mathbf{K}_s^0$（稳定化质量矩阵）：通过 $(\mathbf{I}-\mathbf{\Pi}_{k,E}^0)$ 补充未被投影捕获的自由度，乘以 $|E|$ 保证量纲
- $\mathbf{K}_0$（总质量矩阵）：两部分之和，满足 VEM 的一致性和稳定性

**与公式(45)的联系：** 公式(45)指出 $\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega = \mathbf{K}_0$，将形函数积分与公式(30)的矩阵联系起来，为张量积计算（公式(44)）奠定基础。

---

#### 公式 (31)：$H^1$ 投影矩阵的定义

$$\begin{aligned}
\mathbf{K}_c^\nabla &:= (\mathbf{\Pi}_{\nabla}^{k,E*})^T\int_{\mathcal{K}_x}\nabla\boldsymbol{m}\nabla\boldsymbol{m}^T\,\mathrm{d}\Omega\;\mathbf{\Pi}_{\nabla}^{k,E*} \\
\mathbf{K}_s^\nabla &:= (\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E})^T(\mathbf{I}-\mathbf{\Pi}_{\nabla}^{k,E}) \\
\mathbf{K}_\nabla &:= \mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla
\end{aligned}$$

**与公式(30)的对比：**
- $\mathbf{K}_c^\nabla$ 对应刚度矩阵（$\nabla\boldsymbol{m}\nabla\boldsymbol{m}^T$ 积分）
- $\mathbf{K}_s^\nabla$ 的稳定化项无 $|E|$ 因子（因为梯度型双线性型已具有正确量纲）
- $\mathbf{K}_\nabla$ 将在公式(37)的应变能双线性型中起核心作用

---

#### 公式 (32)：向量位移场的分量表示

$$\boldsymbol{u} = [u_x,\, u_y]^T, \qquad \delta\boldsymbol{u} = [\delta u_x,\, \delta u_y]^T$$

**说明：** 这是二维弹性问题中将向量场按分量分解的标准写法。VEM 对每个分量独立施加投影，类似于 FEM 中的分量组装方式。这为公式(33)-(37)的向量化矩阵构造奠定基础。

---

#### 公式 (33)：速度场的质量双线性型（向量化）

$$m_h^E(\delta\boldsymbol{v},\boldsymbol{v}) = \int_{\mathcal{K}_x}\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega = \begin{bmatrix}\delta\hat{v}_x^T & \delta\hat{v}_y^T\end{bmatrix}\begin{bmatrix}\mathbf{K}_0 & \\ & \mathbf{K}_0\end{bmatrix}\begin{bmatrix}\hat{v}_x \\ \hat{v}_y\end{bmatrix} = \delta\hat{v}^T\mathbb{K}_0\hat{v}$$

$$\mathbb{K}_0 := \begin{bmatrix}\mathbf{K}_0 & \\ & \mathbf{K}_0\end{bmatrix}$$

**推导说明：** 利用向量场的分量独立性：

$$\int_{\mathcal{K}_x}\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega = \int_{\mathcal{K}_x}\delta v_x v_x\,\mathrm{d}\Omega + \int_{\mathcal{K}_x}\delta v_y v_y\,\mathrm{d}\Omega$$

每个分量的积分都等于 $\delta\hat{v}_{(\cdot)}^T\mathbf{K}_0\hat{v}_{(\cdot)}$（由公式(29)），合并成块对角矩阵形式即得。

$\mathbb{K}_0$ 是二维向量场的质量矩阵，在公式(44)-(54)中与时间积分矩阵做 Kronecker 积，最终得到时空单元刚度矩阵。

---

#### 公式 (34)：Voigt 记号下的应变表示

$$\tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})$$

**说明：** Voigt 记号将对称二阶张量 $\epsilon$ 转化为向量 $\tilde{\boldsymbol{\varepsilon}} = [\varepsilon_{xx}, \varepsilon_{yy}, 2\varepsilon_{xy}]^T$（二维情形），从而将张量运算简化为矩阵-向量乘法。$\mathbf{A}$ 是转换矩阵（公式(35)），$\boldsymbol{d}(\boldsymbol{u})$ 是位移偏导数向量（公式(35)）。

---

#### 公式 (35)：转换矩阵 $\mathbf{A}$ 与偏导数向量 $\boldsymbol{d}$

$$\mathbf{A} = \begin{bmatrix}1 & 0 & 0 & 0 \\ 0 & 0 & 0 & 1 \\ 0 & 1 & 1 & 0\end{bmatrix}, \qquad \boldsymbol{d}(\boldsymbol{u}) = \begin{bmatrix}\dfrac{\partial u_x}{\partial x} & \dfrac{\partial u_x}{\partial y} & \dfrac{\partial u_y}{\partial x} & \dfrac{\partial u_y}{\partial y}\end{bmatrix}^T$$

**推导说明：** 由小应变定义（公式(8)）：

$$\varepsilon_{xx} = \frac{\partial u_x}{\partial x}, \quad \varepsilon_{yy} = \frac{\partial u_y}{\partial y}, \quad \varepsilon_{xy} = \frac{1}{2}\!\left(\frac{\partial u_x}{\partial y}+\frac{\partial u_y}{\partial x}\right)$$

在 Voigt 表示中 $\tilde{\boldsymbol{\varepsilon}} = [\varepsilon_{xx}, \varepsilon_{yy}, 2\varepsilon_{xy}]^T$，故：

$$\tilde{\boldsymbol{\varepsilon}} = \begin{bmatrix}1&0&0&0\\0&0&0&1\\0&1&1&0\end{bmatrix}\begin{bmatrix}\partial u_x/\partial x\\ \partial u_x/\partial y\\ \partial u_y/\partial x\\ \partial u_y/\partial y\end{bmatrix} = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})$$

这与公式(34)完全一致。$\mathbf{A}$ 将偏导数向量 $\boldsymbol{d}$（含4个分量）映射到应变向量 $\tilde{\boldsymbol{\varepsilon}}$（含3个独立分量）。

---

#### 公式 (36)：向量场投影的矩阵表示

$$\Pi_{\nabla}^{k,E}\boldsymbol{v}_h = \begin{bmatrix}\boldsymbol{m}^T & \\ & \boldsymbol{m}^T\end{bmatrix}\begin{bmatrix}\mathbf{\Pi}_{\nabla}^{k,E*} & \\ & \mathbf{\Pi}_{\nabla}^{k,E*}\end{bmatrix}\begin{bmatrix}\hat{u}_h^x \\ \hat{u}_h^y\end{bmatrix} = \mathbf{M}^T\mathbf{\Pi}_{\nabla}^*\hat{\boldsymbol{u}}_h$$

**推导说明：** 对向量场的每个分量分别施加公式(24)的投影：

$$\Pi_{\nabla}^{k,E}u_h^x = (\boldsymbol{m}^T\mathbf{\Pi}_{\nabla}^{k,E*})\hat{u}_h^x, \qquad \Pi_{\nabla}^{k,E}u_h^y = (\boldsymbol{m}^T\mathbf{\Pi}_{\nabla}^{k,E*})\hat{u}_h^y$$

写成块矩阵形式，其中 $\mathbf{M}^T := \mathrm{diag}(\boldsymbol{m}^T, \boldsymbol{m}^T)$，$\mathbf{\Pi}_\nabla^* := \mathrm{diag}(\mathbf{\Pi}_\nabla^{k,E*}, \mathbf{\Pi}_\nabla^{k,E*})$。这个表示为公式(37)中应变能双线性型的矩阵推导奠定基础。

---

#### 公式 (37)：应变能双线性型（向量化）

$$\begin{aligned}
a_h^E(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_{\mathcal{K}_x}\varepsilon(\delta\boldsymbol{u}):\mathbb{D}:\varepsilon(\boldsymbol{u})\,\mathrm{d}\Omega = \int_{\mathcal{K}_x}\boldsymbol{d}(\delta\boldsymbol{u})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})\,\mathrm{d}\Omega \\
&= \delta\hat{\boldsymbol{u}}^T\!\left((\mathbf{\Pi}_\nabla^*)^T\!\int_{\mathcal{K}_x}\nabla\mathbf{M}\,\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\nabla\mathbf{M}^T\mathrm{d}\Omega\;\mathbf{\Pi}_\nabla^* + \gamma\begin{bmatrix}\mathbf{K}_s^\nabla & \\ & \mathbf{K}_s^\nabla\end{bmatrix}\right)\hat{\boldsymbol{u}} \\
&= \delta\hat{\boldsymbol{u}}^T(\mathbf{K}_c^\varepsilon + \gamma\mathbf{K}_s^\varepsilon)\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^T\mathbf{K}_\varepsilon\hat{\boldsymbol{u}}
\end{aligned}$$

**推导过程：**

**第一步**（Voigt 转换）：利用公式(34)-(35)，将张量双缩并 $\varepsilon(\delta\boldsymbol{u}):\mathbb{D}:\varepsilon(\boldsymbol{u})$ 转化为矩阵乘法 $\boldsymbol{d}^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\boldsymbol{d}$，其中 $\tilde{\mathbb{D}}$ 是弹性张量的 Voigt 矩阵表示（$3\times3$ 对称矩阵）。

**第二步**（投影代入）：用公式(36)代入 $\boldsymbol{d}(\Pi_\nabla^{k,E}\boldsymbol{u}_h) = \nabla\mathbf{M}^T\mathbf{\Pi}_\nabla^*\hat{\boldsymbol{u}}_h$：

$$\int_{\mathcal{K}_x}\boldsymbol{d}(\Pi_\nabla^{k,E}\delta\boldsymbol{u}_h)^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\boldsymbol{d}(\Pi_\nabla^{k,E}\boldsymbol{u}_h)\,\mathrm{d}\Omega = \delta\hat{\boldsymbol{u}}^T(\mathbf{\Pi}_\nabla^*)^T\underbrace{\int_{\mathcal{K}_x}\nabla\mathbf{M}\,\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\nabla\mathbf{M}^T\mathrm{d}\Omega}_{=:\mathbf{G}_\varepsilon}\mathbf{\Pi}_\nabla^*\hat{\boldsymbol{u}}$$

**第三步**（稳定化项）：稳定化参数取 $\gamma = \frac{1}{4N_K}\mathrm{tr}(\mathbf{K}_c^\varepsilon)$，其中 $N_K$ 是单元角点数，保证稳定项与一致项同阶。

**与公式(56)的联系（FEM 对比）：** 在有限元法中，应变能刚度矩阵由公式(56)给出 $\mathbf{K}^\epsilon = \int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega$，其中 $\mathbf{B}$ 矩阵（公式(57)）对应 VEM 中的 $\nabla\mathbf{M}\,\mathbf{A}^T$ 加投影的部分，两者结构上完全对应。

---

### 3.4 时空弹性问题的矩阵形式

---

#### 公式 (38)：时空单元形函数（张量积结构）

$$\Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x^b}\Phi_{\kappa_t}(1) \\ \Phi_{\kappa_x^t}\Phi_{\kappa_t}(2)\end{bmatrix}$$

**推导说明：** 在张量积网格 $\kappa = \kappa_x\times\kappa_t$ 上，时空形函数是空间形函数与时间形函数的**张量积**：

$$\Phi_\kappa(\boldsymbol{x},t) = \Phi_{\kappa_x}(\boldsymbol{x})\otimes\Phi_{\kappa_t}(t)$$

具体地，时空单元有"下底面"（时刻 $t_n$，标记为上标 $b$）和"上底面"（时刻 $t_{n+1}$，标记为上标 $t$），对应两个时间节点的形函数 $\Phi_{\kappa_t}(1)$ 和 $\Phi_{\kappa_t}(2)$（见公式(39)）。由于上下底面形状相同，$\Phi_{\kappa_x^b} = \Phi_{\kappa_x^t} =: \Phi_{\kappa_x}$。

**与公式(39)-(40)的联系：** 公式(38)给出了张量积形函数的块向量形式，公式(39)定义了时间插值函数，公式(40)给出了梯度表达式。

---

#### 公式 (39)：一维时间线单元的线性插值函数

$$\Phi_{\kappa_t}(1) = \frac{1-\xi}{2}, \qquad \Phi_{\kappa_t}(2) = \frac{1+\xi}{2}$$

**推导说明：** 这是标准的一维线性等参元形函数，$\xi\in[-1,1]$ 为参考坐标，与时间区间 $[t_n, t_{n+1}]$ 通过映射 $t = \frac{(1-\xi)t_n + (1+\xi)t_{n+1}}{2}$ 联系。

- 节点1（$t=t_n$，即 $\xi=-1$）：$\Phi_{\kappa_t}(1)=1$，$\Phi_{\kappa_t}(2)=0$（下底面）
- 节点2（$t=t_{n+1}$，即 $\xi=1$）：$\Phi_{\kappa_t}(1)=0$，$\Phi_{\kappa_t}(2)=1$（上底面）

满足插值性质（Kronecker delta 性质），是有限元时间离散的标准选择。

---

#### 公式 (40)：时空形函数的梯度

$$\nabla_x\Phi_\kappa = \begin{bmatrix}\nabla_x\Phi_{\kappa_x^b}\Phi_{\kappa_t}(1) \\ \nabla_x\Phi_{\kappa_x^t}\Phi_{\kappa_t}(2)\end{bmatrix}, \qquad \nabla_t\Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x^b}\nabla_t\Phi_{\kappa_t}(1) \\ \Phi_{\kappa_x^t}\nabla_t\Phi_{\kappa_t}(2)\end{bmatrix}$$

**推导说明：** 对公式(38)的张量积形函数求偏导数：

**空间梯度**（$\nabla_x$ 只作用于 $\Phi_{\kappa_x}$，$\Phi_{\kappa_t}$ 视为常数）：
$$\nabla_x[\Phi_{\kappa_x}\Phi_{\kappa_t}(i)] = (\nabla_x\Phi_{\kappa_x})\Phi_{\kappa_t}(i)$$

**时间梯度**（$\nabla_t$ 只作用于 $\Phi_{\kappa_t}$，$\Phi_{\kappa_x}$ 视为常数）：
$$\nabla_t[\Phi_{\kappa_x}\Phi_{\kappa_t}(i)] = \Phi_{\kappa_x}(\nabla_t\Phi_{\kappa_t}(i))$$

这与多元函数的乘积法则完全一致。时间导数为 $\nabla_t\Phi_{\kappa_t}(1) = -\frac{1}{h}$，$\nabla_t\Phi_{\kappa_t}(2) = \frac{1}{h}$（$h$ 为时间步长），对应公式(39)的微分。

---

#### 公式 (41)：时空单元位移插值

$$U_h = \begin{bmatrix}\Phi_K^T & \\ & \Phi_K^T\end{bmatrix}\hat{U}_h =: \boldsymbol{\Phi}_K^T\hat{U}_h$$

**说明：** 二维向量位移场 $(u_x, u_y)$ 在时空单元上的插值展开，$x$ 分量和 $y$ 分量分别使用相同的时空形函数 $\Phi_K$（块对角结构）。

---

#### 公式 (42)：时空自由度向量

$$\boldsymbol{\Phi}_K^T := \begin{bmatrix}\Phi_K^T & \\ & \Phi_K^T\end{bmatrix}, \qquad \hat{U}_h := \begin{bmatrix}\hat{u}_x^b & \hat{u}_x^t & \hat{u}_y^b & \hat{u}_y^t\end{bmatrix}^T$$

**说明：** $\hat{U}_h$ 包含时空单元的所有位移自由度：下底面（时刻 $t_n$）的 $x$ 和 $y$ 位移自由度向量，以及上底面（时刻 $t_{n+1}$）的 $x$ 和 $y$ 位移自由度向量。这种排列方式与 Kronecker 积运算的规律相配合（见公式(44)-(54)）。

---

#### 公式 (43)：时空单元刚度矩阵的块结构

$$\mathbf{K} := \begin{bmatrix}\mathbf{K}_{vv} & \mathbf{K}_{vu} \\ \mathbf{K}_{uv} & \mathbf{K}_{uu}\end{bmatrix}$$

**物理含义：** 时空单元刚度矩阵按速度自由度（$\boldsymbol{v}$）和位移自由度（$\boldsymbol{u}$）分块，对应混合公式(15)中的两个方程：

- $\mathbf{K}_{vv}$：速度-速度耦合（对应公式(18)的 $A_{vv}$，质量矩阵类）
- $\mathbf{K}_{vu}$：速度-位移耦合（对应公式(18)的 $A_{vu}$，约束/耦合项）
- $\mathbf{K}_{uv}$：位移-速度耦合（对应公式(18)的 $A_{uv}$，惯性项）
- $\mathbf{K}_{uu}$：位移-位移耦合（对应公式(18)的 $A_{uu}$，刚度矩阵类）

各子矩阵的显式 Kronecker 积公式见公式(51)-(54)。

---

#### 公式 (44)：速度-速度刚度矩阵的 Kronecker 积分解

$$\begin{aligned}
\mathbf{K}_{\boldsymbol{v}\boldsymbol{v}} &= \rho^{-1}\int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}(\Phi_\mathcal{K}+\theta h\nabla_t\Phi_\mathcal{K})\Phi_\mathcal{K}^T\,\mathrm{d}\Omega\,\mathrm{d}t \\
&= \rho^{-1}\begin{bmatrix}\displaystyle\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\mathrm{d}\Omega & \\ & \displaystyle\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\mathrm{d}\Omega\end{bmatrix} \\
&\quad + \rho^{-1}\theta h\begin{bmatrix}\displaystyle\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\mathrm{d}\Omega & \\ & \displaystyle\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t\otimes\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\mathrm{d}\Omega\end{bmatrix}
\end{aligned}$$

**推导过程（Kronecker 积的来源）：** 利用张量积形函数（公式(38)），时空积分可分解为空间积分与时间积分的乘积：

$$\int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}\Phi_\mathcal{K}\Phi_\mathcal{K}^T\,\mathrm{d}\Omega\,\mathrm{d}t = \underbrace{\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t}_{=\mathbf{L}}\otimes\underbrace{\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\mathrm{d}\Omega}_{=\mathbf{K}_0}$$

迎风项含 $\nabla_t\Phi_\mathcal{K}$，利用公式(40)中的时间梯度：

$$\int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}\theta h\nabla_t\Phi_\mathcal{K}\Phi_\mathcal{K}^T\,\mathrm{d}\Omega\,\mathrm{d}t = \theta h\underbrace{\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\mathrm{d}t}_{=\mathbf{Q}}\otimes\mathbf{K}_0$$

Kronecker 积的定义：$\mathbf{A}\otimes\mathbf{B} = (a_{ij}\mathbf{B})$（即 $\mathbf{A}$ 的每个元素与 $\mathbf{B}$ 的乘积构成分块矩阵）。

**计算效率优势：** 由于时间积分矩阵（$\mathbf{L}$、$\mathbf{Q}$ 等）和空间积分矩阵（$\mathbf{K}_0$、$\mathbf{K}_\varepsilon$ 等）分别只需计算一次，整个时空刚度矩阵可通过 Kronecker 积直接组装，极大地提高了计算效率。

---

#### 公式 (45)：空间形函数质量矩阵与 VEM 矩阵的对应

$$\int_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}\boldsymbol{\phi}_{\mathcal{K}_x}^T\,\mathrm{d}\Omega = \mathbf{K}_c^0 + \mathbf{K}_s^0 =: \mathbf{K}_0$$

**意义：** 将公式(44)中出现的空间形函数质量矩阵积分与公式(30)定义的 VEM 矩阵 $\mathbf{K}_0$ 直接对应起来，是张量积计算框架（VEM 空间 + FEM 时间）在矩阵层面的桥梁。

对于有限元法，$\int_{\kappa_x}\phi_\mathrm{FEM}\phi_\mathrm{FEM}^T\,\mathrm{d}\Omega$ 是直接可计算的精确质量矩阵（公式(55)）；对于虚元法，用公式(30)中的 VEM 近似 $\mathbf{K}_0$ 代替，形式上完全统一。

---

#### 公式 (46)-(49)：时间方向积分矩阵的高斯求积

$$\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^{2}w(i)\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{L}$$

$$\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^{2}w(i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{Q}$$

$$\int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^{2}w(i)\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{R}$$

$$\int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^{2}w(i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{W}$$

**推导与计算说明：**

**坐标变换**：时间区间 $[t_n, t_{n+1}]$ 映射到参考区间 $[-1,1]$，Jacobi 行列式为 $\mathrm{d}t = \frac{h}{2}\mathrm{d}\xi$（$h=t_{n+1}-t_n$ 为时间步长），故积分前有系数 $\frac{h}{2}$。

**两点 Gauss-Legendre 求积**：高斯点为 $\xi_1 = -\frac{1}{\sqrt{3}}$，$\xi_2 = \frac{1}{\sqrt{3}}$，权重均为 $w(1) = w(2) = 1$，对多项式精确到 $2\times2-1=3$ 次（对线性形函数积分精确）。

**解析计算（线性形函数，$h=t_{n+1}-t_n$）：**

由公式(39)：$\boldsymbol{\phi}_{\mathcal{K}_t} = \left[\frac{1-\xi}{2}, \frac{1+\xi}{2}\right]^T$，$\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t} = \left[-\frac{1}{h}, \frac{1}{h}\right]^T$，积分结果为：

$$\mathbf{L} = \frac{h}{6}\begin{bmatrix}2 & 1 \\ 1 & 2\end{bmatrix}, \quad \mathbf{Q} = \frac{1}{2}\begin{bmatrix}-1 & -1 \\ 1 & 1\end{bmatrix}, \quad \mathbf{R} = \frac{1}{2}\begin{bmatrix}-1 & 1 \\ -1 & 1\end{bmatrix}, \quad \mathbf{W} = \frac{1}{h}\begin{bmatrix}1 & -1 \\ -1 & 1\end{bmatrix}$$

注意 $\mathbf{R} = \mathbf{Q}^T$，$\mathbf{W}$ 正比于单位矩阵减去全1矩阵，这些矩阵都是纯数字矩阵，只计算一次即可用于所有时间单元。

**各矩阵的物理含义：**
- $\mathbf{L}$（一致质量矩阵型）：时间形函数的 $L^2$ 内积，用于公式(51)-(53)中的质量项
- $\mathbf{Q}$（迎风梯度-形函数型）：时间形函数梯度与形函数的内积，用于迎风稳定化项
- $\mathbf{R}$（形函数-梯度型，$=\mathbf{Q}^T$）：用于耦合项
- $\mathbf{W}$（刚度矩阵型）：时间形函数梯度的 $L^2$ 内积，类似时间方向"刚度矩阵"

---

#### 公式 (50)：分块矩阵的记号约定

$$\mathbf{K}_{\mathrm{any}} = \begin{bmatrix}\mathbf{K}_{\mathrm{any}}^{I,I} & \mathbf{K}_{\mathrm{any}}^{I,II} \\ \mathbf{K}_{\mathrm{any}}^{II,I} & \mathbf{K}_{\mathrm{any}}^{II,II}\end{bmatrix}$$

**说明：** 定义矩阵的分块记号，用于在公式(51)-(54)中方便地提取二维向量场质量矩阵（$\mathbb{K}_0$）或刚度矩阵（$\mathbf{K}_\varepsilon$）的各分块（$x$-$x$、$x$-$y$、$y$-$x$、$y$-$y$ 分量）。

对于对角块矩阵 $\mathbb{K}_0 = \mathrm{diag}(\mathbf{K}_0,\mathbf{K}_0)$：$\mathbf{K}_0^{I,I} = \mathbf{K}_0$，$\mathbf{K}_0^{II,II} = \mathbf{K}_0$，交叉项为零。

---

#### 公式 (51)：速度-速度子矩阵（Kronecker 积简洁形式）

$$\mathbf{K}_{\boldsymbol{v}\boldsymbol{v}} = \rho^{-1}\begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{L}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \rho^{-1}\theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{Q}\otimes\mathbf{K}_0^{II,II}\end{bmatrix}$$

**推导说明：** 将公式(44)中的积分用公式(45)-(47)代入（$\mathbf{K}_0$、$\mathbf{L}$、$\mathbf{Q}$），并利用公式(50)的分块记号整理，即得此简洁形式。

**物理意义：**
- 第一项 $\rho^{-1}(\mathbf{L}\otimes\mathbf{K}_0)$：时空单元的质量矩阵（速度场），$\rho^{-1}$ 前缀来自公式(18)中 $A_{vv}$ 的 $\rho$ 系数（注意原公式是 $-\rho$，这里的符号约定与方程整理方式有关）
- 第二项 $\rho^{-1}\theta h(\mathbf{Q}\otimes\mathbf{K}_0)$：迎风稳定化贡献，$\mathbf{Q}$ 包含时间方向的迎风信息

---

#### 公式 (52)：速度-位移耦合子矩阵

$$\mathbf{K}_{\boldsymbol{v}\boldsymbol{u}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix}$$

**推导说明：** 对应公式(18)中 $A_{vu}(\delta\boldsymbol{v},\boldsymbol{u})$ 的离散化。包含 $\nabla_t\boldsymbol{u}$（位移的时间导数），故时间积分矩阵中含 $\nabla_t\boldsymbol{\phi}_{\mathcal{K}_t}$（对应 $\mathbf{R}$），迎风项含 $\nabla_t(\delta\boldsymbol{v})\cdot\nabla_t\boldsymbol{u}$（对应 $\mathbf{W}$）。

---

#### 公式 (53)：位移-速度耦合子矩阵

$$\mathbf{K}_{\boldsymbol{u}\boldsymbol{v}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix}$$

**注意：** 公式(52)和(53)在形式上完全相同，但对应不同的方程（$A_{vu}$ 和 $A_{uv}$）。这说明在对称化处理后，速度-位移与位移-速度的耦合矩阵具有相同的结构。这一对称性来源于公式(16)中 $A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u})$ 和 $A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v})$ 在离散化后的结构对称性。

---

#### 公式 (54)：位移-位移子矩阵（刚度矩阵）

$$\mathbf{K}_{\boldsymbol{u}\boldsymbol{u}} = \begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,II} \\ \mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,II} \\ \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix}$$

**推导说明：** 对应公式(18)中 $A_{uu}(\delta\boldsymbol{u},\boldsymbol{u})$ 的离散化（含迎风项）。与 $\mathbf{K}_{vv}$ 结构类似，但用应变刚度矩阵 $\mathbf{K}_\varepsilon$（公式(37)）代替质量矩阵 $\mathbf{K}_0$。

**与 $\mathbf{K}_{vv}$ 的区别：**
- $\mathbf{K}_{vv}$ 用 $\mathbf{K}_0$（质量矩阵，$x$-$y$ 解耦，块对角）
- $\mathbf{K}_{uu}$ 用 $\mathbf{K}_\varepsilon$（刚度矩阵，$x$-$y$ 耦合，非对角块）

$\mathbf{K}_\varepsilon$ 的非对角项（$\mathbf{K}_\varepsilon^{I,II}$ 等）对应剪切变形产生的 $x$-$y$ 耦合效应（通过弹性张量 $\mathbb{D}$ 的 Voigt 矩阵 $\tilde{\mathbb{D}}$ 体现）。

---

## 第4节：空间-时间动力学的有限元法

---

#### 公式 (55)：FEM 质量矩阵

$$\mathbf{K}^0 = \int_{\kappa_x}[\phi_\mathrm{FEM}][\phi_\mathrm{FEM}]^T\,\mathrm{d}\Omega$$

**说明：** 这是有限元方法中经典的单元质量矩阵（一致质量矩阵，consistent mass matrix）。$\phi_\mathrm{FEM}$ 是 FEM 形函数向量（对一阶四边形单元为 $[N_1, N_2, N_3, N_4]^T$）。

**与 VEM 的对比（公式(45)）：** 在 FEM 中，$\mathbf{K}^0$ 可以直接精确计算；在 VEM 中，形函数 $\phi_\mathrm{VEM}$ 不显式，$\mathbf{K}^0 = \mathbf{K}_0 = \mathbf{K}_c^0 + \mathbf{K}_s^0$（公式(30)），通过投影和稳定化近似。

---

#### 公式 (56)：FEM 刚度矩阵

$$\mathbf{K}^\epsilon = \int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega$$

**推导说明：** 这是 FEM 中的标准单元刚度矩阵。$\mathbf{B}$ 为应变-位移矩阵（公式(57)），$\tilde{\mathbb{D}}$ 为弹性矩阵（Voigt 形式的 $3\times3$ 对称矩阵）。

利用 Voigt 记号，内力虚功 $\delta\boldsymbol{u}^T\int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega\,\boldsymbol{u}$ 等同于 $\int_{\kappa_x}\varepsilon(\delta\boldsymbol{u}):\mathbb{D}:\varepsilon(\boldsymbol{u})\,\mathrm{d}\Omega$，与公式(37)结构完全对应。

**与 VEM 的联系（公式(37)）：** 在 FEM 中，$\mathbf{K}^\epsilon = \int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega$ 精确（无稳定化）；在 VEM 中，$\mathbf{K}_\varepsilon = \mathbf{K}_c^\varepsilon + \gamma\mathbf{K}_s^\varepsilon$，含稳定化项（因为 VEM 形函数需要投影处理）。两者都扮演"刚度矩阵"的角色，最终代入相同的 Kronecker 积公式(51)-(54)得到时空刚度矩阵。

---

#### 公式 (57)：FEM 的 $\mathbf{B}$ 矩阵（应变-位移矩阵）

$$\mathbf{B} := \begin{bmatrix}\dfrac{\partial\phi_\mathrm{FEM}^T}{\partial x} & \mathbf{0}^T \\[6pt] \mathbf{0}^T & \dfrac{\partial\phi_\mathrm{FEM}^T}{\partial y} \\[6pt] \dfrac{\partial\phi_\mathrm{FEM}^T}{\partial y} & \dfrac{\partial\phi_\mathrm{FEM}^T}{\partial x}\end{bmatrix}$$

**推导说明：** $\mathbf{B}$ 矩阵将节点位移自由度 $\hat{\boldsymbol{u}} = [\hat{u}_x^T, \hat{u}_y^T]^T$ 映射到 Voigt 应变 $\tilde{\boldsymbol{\varepsilon}} = [\varepsilon_{xx}, \varepsilon_{yy}, 2\varepsilon_{xy}]^T$：

$$\tilde{\boldsymbol{\varepsilon}} = \mathbf{B}\hat{\boldsymbol{u}}$$

其中：
- 第1行：$\varepsilon_{xx} = \sum_i\frac{\partial N_i}{\partial x}u_{x,i}$（$x$ 方向正应变）
- 第2行：$\varepsilon_{yy} = \sum_i\frac{\partial N_i}{\partial y}u_{y,i}$（$y$ 方向正应变）
- 第3行：$2\varepsilon_{xy} = \sum_i\frac{\partial N_i}{\partial y}u_{x,i} + \frac{\partial N_i}{\partial x}u_{y,i}$（剪切应变）

**与 VEM 中相应量的联系：** 在 VEM 中，$\mathbf{B}$ 的对应量为 $\mathbf{A}\nabla\mathbf{M}^T\mathbf{\Pi}_\nabla^*$（将公式(35)的 $\mathbf{A}$ 矩阵与投影后的梯度结合），但由于 VEM 形函数不显式，需要通过投影算符间接计算，这正是公式(37)的逐步推导所展示的内容。

---

## 公式间内在联系总结

下面从三个维度阐明论文全部公式的内在逻辑联系：

### 一、从 Hamilton 原理到弱形式（公式1→2→…→14）

$$\underbrace{Q = \Omega\times I}_{(1)} \xrightarrow{\text{作用量}} \underbrace{H = \int_I 2\mathcal{K}\,\mathrm{d}t}_{(2)} \xrightarrow{\text{动能}} \underbrace{\mathcal{K} = \int_\Omega\frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2\mathrm{d}V}_{(3)}$$

$$\xrightarrow{\text{热力学第一定律}} \underbrace{\int_I\mathcal{K}\,\mathrm{d}t+\int_I\mathcal{G}\,\mathrm{d}t=0}_{(4)} \xrightarrow{\text{势能}} \underbrace{\mathcal{G} = \int_\Omega\boldsymbol{\psi}\,\mathrm{d}V - \text{外力功}}_{(5)}$$

$$\xrightarrow{\text{应变能密度}} \underbrace{\boldsymbol{\psi}=\frac{1}{2}\epsilon:\mathbb{D}:\epsilon}_{(6)} \xrightarrow{\text{弹性张量}} \underbrace{\mathbb{D}_{ijkl}}_{(7)} \xrightarrow{\text{小应变}} \underbrace{\epsilon=\nabla^{\mathrm{sym}}\boldsymbol{u}}_{(8)}$$

$$\xrightarrow{\text{Hamilton 原理}} \underbrace{H=\int_I(\mathcal{K}-\mathcal{G})\,\mathrm{d}t}_{(9)} \xrightarrow{\text{混合公式}} \underbrace{\mathcal{K}[\boldsymbol{v},\boldsymbol{u}]}_{(10)} \xrightarrow{\delta H=0} \underbrace{(11)\to(12)\to(13)\to(14)}_{\text{弱形式}}$$

### 二、从弱形式到离散双线性形式（公式14→…→37）

$$\underbrace{(14)\text{ 弱形式}}_{} \xrightarrow{\text{双线性型}} \underbrace{(15)(16)(17)}_{} \xrightarrow{\text{迎风稳定化}} \underbrace{(18)(19)}_{}$$

$$\xrightarrow{\text{VEM 投影}} \underbrace{(20)(21)(22)(23)(24)(25)}_{H^1\text{ 和 }L^2\text{ 投影}} \xrightarrow{\text{离散双线性型}} \underbrace{(27)(28)(29)}_{}$$

$$\xrightarrow{\text{矩阵定义}} \underbrace{(30)(31)}_{\mathbf{K}_0,\,\mathbf{K}_\nabla} \xrightarrow{\text{向量化}} \underbrace{(32)(33)(34)(35)(36)(37)}_{\mathbb{K}_0,\,\mathbf{K}_\varepsilon}$$

### 三、从离散双线性形式到时空单元刚度矩阵（公式37→…→54）

$$\underbrace{(33)(37)}_{\mathbf{K}_0,\,\mathbf{K}_\varepsilon} \xrightarrow{\text{张量积形函数}} \underbrace{(38)(39)(40)(41)(42)}_{} \xrightarrow{\text{时空积分分离}} \underbrace{(44)(45)}_{}$$

$$\xrightarrow{\text{高斯积分}} \underbrace{(46)(47)(48)(49)}_{\mathbf{L},\mathbf{Q},\mathbf{R},\mathbf{W}} \xrightarrow{\text{Kronecker 积}} \underbrace{(43)(50)(51)(52)(53)(54)}_{\text{时空刚度矩阵}}$$

FEM 版本的刚度矩阵路径：$\underbrace{(55)(56)(57)}_{\mathbf{K}^0,\,\mathbf{K}^\epsilon,\,\mathbf{B}} \xrightarrow{\text{代入}} \underbrace{(51)-(54)}_{\text{时空刚度矩阵（FEM）}}$

### 四、VEM 与 FEM 的统一框架

公式(51)-(54)提供了一个统一的时空单元刚度矩阵框架，其中：
- 使用 VEM：$\mathbf{K}_0 = \mathbf{K}_c^0+\mathbf{K}_s^0$（公式(30)），$\mathbf{K}_\varepsilon = \mathbf{K}_c^\varepsilon+\gamma\mathbf{K}_s^\varepsilon$（公式(37)）
- 使用 FEM：$\mathbf{K}_0 = \int\phi_\mathrm{FEM}\phi_\mathrm{FEM}^T\,\mathrm{d}\Omega$（公式(55)），$\mathbf{K}_\varepsilon = \int\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega$（公式(56)-(57)）

两者的时间积分矩阵 $\mathbf{L}$、$\mathbf{Q}$、$\mathbf{R}$、$\mathbf{W}$（公式(46)-(49)）完全相同，只有空间矩阵 $\mathbf{K}_0$ 和 $\mathbf{K}_\varepsilon$ 的计算方式不同（VEM 用投影近似，FEM 用精确积分）。这正是论文"结构化数值方法"（structured numerical approach）的精髓——将空间离散（VEM 或 FEM）与时间离散（FEM）通过 Kronecker 积解耦，最大化计算效率。

---

*本文档完整覆盖论文中公式编号 (1) 至 (57) 的全部公式，逐一给出了推导过程、物理含义，并系统阐明了各公式之间的内在联系。*
