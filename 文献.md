# Space-time virtual element method for elastodynamics: Theory, applications, and code development

**作者**: Bing-Bing Xu, Philipp Junker, Peter Wriggers  
**机构**: Institute of Continuum Mechanics, Leibniz Universität Hannover, Hannover, Germany

---

## 摘要

**关键词**: 虚拟元方法；动力学；时空

本文提出了一种时空虚拟元方法，用于在时空柱体中对二维弹性动力学问题进行离散化。时空格式的基本思想是将时间视为一个额外的维度。该方法已基于有限元法和虚拟元法广泛应用于众多初值问题。然而，目前尚未有关于时空虚拟元方法在弹性动力学中应用的报道。基于哈密顿原理推导了变分公式和时空双线性格式。离散空间通过空间上的虚拟元方法和时间上的迎风有限元方法获得。时空网格作为空间网格和时间网格的张量积得到，然后弹性动力学问题的单元刚度矩阵可以直接基于克罗内克积获得。通过几个二维算例验证了所发展的时空虚拟元方法的精度和稳定性。本文开发的时空虚拟元法和有限元法的 MATLAB 代码可从 https://github.com/Qinxiaoye/VEM-space-time-dynamic 下载。

---

## 1. 引言

虚拟元方法（VEM）[1,2] 是有限元法（FEM）向一般多边形或多面体单元的推广。与有限元法和其他多边形有限元方法不同，虚拟元方法通过定义从虚拟元空间到多项式空间的投影，避免了在多边形单元中显式计算形函数。鉴于虚拟元方法在单元形状和自由度定义方面的灵活性，该方法已被应用于各种不同的领域。当前的应用包括线弹性力学问题 [3-8]、板壳问题 [9-11]、有限变形下的超弹性材料 [12-15]、弹塑性力学问题 [16]、接触问题 [17-20] 和有限弹塑性变形 [21-23]。详细信息请参阅最新的工程 VEM 书籍 [24]。

虚拟元方法也被发展用于求解控制方程为抛物型或双曲型的初边值问题。例如，瞬态热传导问题、波动问题 [25] 和动力学问题 [26-29]。在上述 VEM 文献中，时间相关问题通过空间上的虚拟元离散化和时间上的时间步进格式（显式时间差分、Newmark 方法等）求解。通过适当的参数选择，Newmark 方法是无条件稳定的。然而，它不是严格辛的，这可能导致长期动态模拟中的相位延迟。对于非线性问题，Newmark 方法的每个时间步都需要迭代，导致计算效率较低。

以哈密顿作用量驻值原理作为动力学问题求解的起点有着悠久的历史。Argyris 等人 [30] 提出了最初的公式。由于当时计算能力有限，人们发展了各种不同的增量求解方案，如有限差分法和 Newmark 方法。如今，借助强大的计算机和并行计算技术，哈密顿原理的应用得以复兴，以构建在时空格式中求解时间相关问题的方法。时空格式的基本思想是将时间离散化为一个维度，时间被视为一个额外的空间变量。由于时空格式具有能量守恒、易于并行计算和自适应网格划分等优点，该方法在过去三十年中得到了显著发展 [31-33]。有几种数值方法可用于离散化时空格式，其中有限元法（FEM）最为常见。迄今为止，时空有限元法已应用于动力学问题 [34-36]、声波问题 [37]、弹性波问题 [38]、流体动力学问题 [39,40] 等。近期基于哈密顿原理的工作，通过评估所有热力学状态变量的驻值条件，可以获得热力耦合模型。也可以将时空格式应用于使用 FEM 的弹性波传播、粘弹性和弹塑性问题 [41,42]。除了有限元法，等几何方法在特定应用中也具有优势 [43,44]。

由于虚拟元方法可以使用任意多边形网格（有利于自适应技术），不同的时空 VEM 格式已被用于瞬态热传导方程 [45,46]、耗散波动方程 [47] 和弹性波方程 [48]。然而，到目前为止，尚未有关于时空 VEM 在弹性动力学问题中应用的报道。本文设计并分析了一种用于求解二维动力学问题的时空虚拟元方法。在虚拟元方法中，由于投影算子是基于双线性格式计算的，因此可以使用"结构化"数值方法来避免在时间方向上定义额外的自由度。在"结构化"数值方法中，时空网格作为空间网格和时间网格的张量积获得。对于这个 2+1 维问题，选择了用于"空间"离散化的 VEM 和用于"时间"离散化的 FEM。这种技术的优点是可以直接应用 VEM 中用于静态问题构建投影算子的计算方法。此外，在二维多边形网格和一维时间网格上进行积分，使得最终的刚度矩阵可以直接通过克罗内克积获得。为了确保时空 VEM 的稳定性，引入了时间上迎风格式。

本文分为以下几个部分。第 2 章回顾了基于哈密顿原理的动力学问题变分公式。此外，在本节中，还扩展了时空双线性格式和时间上迎风稳定化。然后，第 3 章讨论了弹性动力学的时空虚拟元方法。讨论了投影算子、稳定化项以及残差和刚度矩阵的形成。为了比较，第 4 章总结了用于时空动力学的有限元方法。第 5 章给出了二维弹性动力学的数值算例。第 6 章提供了结论和讨论。本文开发的代码可从 https://github.com/Qinxiaoye/VEM-space-time-dynamic 下载。

---

## 2. 变分公式

### 2.1 基于哈密顿原理的变分公式

本文采用时空技术考虑线弹性动力学问题。给定一个 Lipschitz 有界域 $\Omega \in \mathbb{R}^n$，其中 $n = 1,2,3$，以及时空柱体

$$
Q \coloneqq \Omega \times I \quad (1)
$$

其中时间区间 $I = (0,T)$，$t\in I$，哈密顿作用量泛函则表示为动量向量 $\pmb{\rho} \coloneqq \rho \nabla_{t}\pmb{u}$ 沿路径 $[\pmb{u}_0,\pmb{u}_T]$ 的积分，即：

$$
H \coloneqq \int_{\Omega}\int_{\pmb{u}_0}^{\pmb{u}_T}\pmb{\rho}\cdot \mathrm{d}\pmb{u}\mathrm{d}V = \int_{\Omega}\int_{0}^{T}\rho \nabla_{t}\pmb{u}\cdot \nabla_{t}\pmb{u}\mathrm{d}t\mathrm{d}V = \int_{I}\int_{\Omega}\rho \| \nabla_{t}\pmb{u}\|^{2}\mathrm{d}V\mathrm{d}t = \int_{I}2\mathcal{K}\mathrm{d}t \quad (2)
$$

其中使用了时间导数的定义 $\frac{\mathrm{d}}{\mathrm{d}t} = \nabla_{t}$。动能引入为：

$$
\mathcal{K} = \int_{\Omega}\frac{1}{2}\rho \| \nabla_{t}\pmb{u}\|^{2}\mathrm{d}V \quad (3)
$$

利用能量平衡（热力学第一定律）可得：

$$
\int_{I}\mathcal{K}\mathrm{d}t + \int_{I}\mathcal{G}\mathrm{d}t = 0 \quad (4)
$$

其中总势能 $\mathcal{G}$ 定义为：

$$
\mathcal{G} \coloneqq \int_{\Omega}\psi\mathrm{d}V - \int_{\Omega}\tilde{\pmb{j}}\cdot \pmb{u}\mathrm{d}V - \int_{\Gamma_{\sigma}}\tilde{\pmb{t}}\cdot \pmb{u}\mathrm{d}\Gamma \quad (5)
$$

这里 $\tilde{\pmb{j}}$ 是体积力，$\tilde{\pmb{t}}$ 是诺伊曼边界 $\Gamma_{\sigma}$ 上的外部压力。此外，

$$
\psi \coloneqq \frac{\lambda}{2}\left(\mathrm{tr}(\epsilon)\right)^{2} + \mu \epsilon:\epsilon = \frac{1}{2}\epsilon:\mathbb{D}:\epsilon \quad (6)
$$

是自由能密度，其中弹性张量 $\mathbb{D}\in \mathbb{R}^{d\times d\times d\times d}$ 为：

$$
\mathbb{D}_{ijkl} = \lambda \delta_{ij}\delta_{kl} + \mu \left(\delta_{ik}\delta_{jl} + \delta_{il}\delta_{jk}\right) \quad (7)
$$

应变张量为：

$$
\epsilon = \frac{1}{2}\left(\nabla \pmb{u} + \nabla \pmb{u}^T\right) =: \nabla^{\mathrm{sym}}\pmb{u} = \epsilon(\pmb{u}) \quad (8)
$$

则哈密顿作用量函数重新表述为：

$$
H = \int_{I}(\mathcal{K} - \mathcal{G})\mathrm{d}t \quad (9)
$$

我们通过引入速度向量 $\nu$ 作为独立变量来引入混合格式。对于这种混合格式，动能为：

$$
\mathcal{K} = \int_{\Omega}\rho \nu \cdot \nabla_{t}\pmb{u} - \frac{1}{2}\rho \| \nu \|^2 \mathrm{d}\Omega \quad (10)
$$

显然，当 $\nu \equiv \nabla_{t}\pmb{u}$ 时，该格式简化为原始动能。

那么，哈密顿作用量驻值原理要求：

$$
H = H[\nu,\pmb{u},\nabla^{\mathrm{sym}}\pmb{u},\nabla_{t}\pmb{u}] \to \mathrm{stat} \Leftrightarrow \delta H = \delta_{\nu}H + \delta_{\pmb{u}}H = 0 \quad \forall \delta\nu,\delta\pmb{u} \quad (11)
$$

由于 $\delta\nu$ 和 $\delta\pmb{u}$ 的独立性，驻值条件可以导出：

$$
\left\{ \begin{array}{ll}
\delta_{\nu}H = 0 & \forall \delta\nu \\
\delta_{\pmb{u}}H = 0 & \forall \delta\pmb{u}
\end{array} \right. \quad (12)
$$

其中：

$$
\left\{ \begin{array}{l}
\delta_{\nu}H = \displaystyle\int_{I}\int_{\Omega}\rho (\nabla_{t}\pmb{u} - \nu)\cdot \delta\nu\,\mathrm{d}V\mathrm{d}t = 0 \quad \forall \delta\nu \\[10pt]
\delta_{\pmb{u}}H = \displaystyle\int_{I}\int_{\Omega}\rho \nu\cdot \nabla_{t}\delta\pmb{u}\,\mathrm{d}V\mathrm{d}t - \int_{I}\left(\int_{\Omega}\left(\pmb{\sigma}:\epsilon(\delta\pmb{u}) - \tilde{\pmb{j}}\cdot\delta\pmb{u}\right)\mathrm{d}V - \int_{\Gamma_{\sigma}}\pmb{t}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t = 0 \quad \forall \delta\pmb{u}
\end{array} \right. \quad (13)
$$

其中应用了应力 $\sigma = \frac{\partial\psi}{\partial\epsilon} = \mathbb{D}:\epsilon$。对 (13) 中的第一项在时间上应用分部积分，将驻值条件转化为：

$$
\left\{ \begin{array}{l}
\displaystyle\int_{I}\int_{\Omega} -\rho \nu \cdot \delta\nu \,\mathrm{d}V\mathrm{d}t + \int_{I}\int_{\Omega}\rho \nabla_{t}\pmb{u}\cdot \delta\nu \,\mathrm{d}V\mathrm{d}t = 0 \quad \forall \delta\nu \\[10pt]
\displaystyle\int_{I}\int_{\Omega}\rho \nabla_{t}\nu \cdot \delta\pmb{u}\,\mathrm{d}V\mathrm{d}t + \int_{I}\int_{\Omega}\sigma:\delta\nabla^{\mathrm{sym}}\pmb{u}\,\mathrm{d}V\mathrm{d}t = \int_{I}\left(\int_{\Omega}\tilde{\pmb{j}}\cdot\delta\pmb{u}\,\mathrm{d}V + \int_{\Gamma_{\sigma}}\pmb{t}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t
\end{array} \right. \quad (14)
$$

其中利用了性质 $\delta\pmb{u}|_{0} = \delta\pmb{u}|_{T} \equiv \mathbf{0}$。注意初始条件（位置、速度）可以直接应用而无需任何进一步修改。

### 2.2 时空双线性格式与时间上迎风稳定化

由于引入了速度 $\nu$ 作为独立变量，式 (14) 是一个双场弱形式方程。上述公式可以通过有限元法 [35,36] 或虚拟元法进行离散。基于式 (14)，双线性形式可写为：

$$
A(\nu,\pmb{u},\delta\nu,\delta\pmb{u}) = \begin{pmatrix}
A^{\mathrm{trv}}(\delta\nu,\nu) + A^{\mathrm{trv}}(\delta\nu,\pmb{u}) \\[5pt]
A^{\mathrm{trv}}(\delta\pmb{u},\nu) + A^{\mathrm{trv}}(\delta\pmb{u},\pmb{u})
\end{pmatrix} = \begin{pmatrix}
\mathbf{0} \\[5pt]
l(\delta\pmb{u})
\end{pmatrix} \quad (15)
$$

其中：

$$
\begin{array}{l}
A^{\mathrm{trv}}(\delta\nu,\nu) = \displaystyle\int_{0}^{T}\int_{\Omega} -\rho \,\delta\nu\cdot\nu \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A^{\mathrm{trv}}(\delta\nu,\pmb{u}) = \displaystyle\int_{0}^{T}\int_{\Omega} \rho \,\delta\nu\cdot\nabla_{t}(\pmb{u}) \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A^{\mathrm{trv}}(\delta\pmb{u},\nu) = \displaystyle\int_{0}^{T}\int_{\Omega} \rho \,\delta\pmb{u}\cdot\nabla_{t}\nu \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A^{\mathrm{trv}}(\delta\pmb{u},\pmb{u}) = \displaystyle\int_{0}^{T}\int_{\Omega} \epsilon(\delta\pmb{u}):\mathbb{D}:\epsilon(\pmb{u}) \,\mathrm{d}\Omega\mathrm{d}t,
\end{array} \quad (16)
$$

以及

$$
l(\delta\pmb{u}) = \int_{0}^{T}\left(\int_{\Omega}\tilde{\pmb{j}}\cdot\delta\pmb{u}\,\mathrm{d}\Omega + \int_{\Gamma_{\sigma}}\pmb{t}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t \quad (17)
$$

对于某些应用，使用基于式 (15) 的公式会导致不稳定的响应。因此，为了确保数值解的稳定性，应应用 [36] 中引入的时间上迎风格式。该格式将测试函数 $\delta\pmb{u}$ 和 $\delta\nu$ 替换为 $\delta\pmb{u} + \theta h \nabla_t(\delta\pmb{u})$ 和 $\delta\nu + \theta h \nabla_t(\delta\nu)$，其中 $\theta$ 是一个正常数，$h$ 是时间方向上的单元长度。式 (15) 中的离散格式现在重新表述为：

$$
\begin{array}{l}
A_{vv}(\delta\nu,\nu) = \displaystyle\int_{0}^{T}\int_{\Omega} -\rho (\delta\nu + \theta h \nabla_t(\delta\nu))\cdot\nu \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A_{vu}(\delta\nu,\pmb{u}) = \displaystyle\int_{0}^{T}\int_{\Omega} \rho (\delta\nu + \theta h \nabla_t(\delta\nu))\cdot\nabla_t(\pmb{u}) \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A_{uv}(\delta\pmb{u},\nu) = \displaystyle\int_{0}^{T}\int_{\Omega} \rho (\delta\pmb{u} + \theta h \nabla_t(\delta\pmb{u}))\cdot\nabla_t\nu \,\mathrm{d}\Omega\mathrm{d}t, \\[10pt]
A_{uu}(\delta\pmb{u},\pmb{u}) = \displaystyle\int_{0}^{T}\int_{\Omega} \epsilon(\delta\pmb{u} + \theta h \nabla_t(\delta\pmb{u})):\mathbb{D}:\epsilon(\pmb{u}) \,\mathrm{d}\Omega\mathrm{d}t,
\end{array} \quad (18)
$$

以及

$$
l(\delta\pmb{u}) = \int_{0}^{T}\left(\int_{\Omega}\tilde{\pmb{j}}\cdot(\delta\pmb{u} + \theta h \nabla_t(\delta\pmb{u}))\,\mathrm{d}\Omega + \int_{\Gamma_{\sigma}}\pmb{t}\cdot(\delta\pmb{u} + \theta h \nabla_t(\delta\pmb{u}))\,\mathrm{d}\Gamma\right)\mathrm{d}t \quad (19)
$$

参数 $\theta$ 的选择将在第 5 节讨论。

在空间维度上使用虚拟元方法进行离散化，在时间维度上选择拉格朗日插值（有限元法中的插值函数）。当然，有限元法也可用于空间维度的离散化，如第 4 节所述。虚拟元方法的基本原理将在下一节介绍。

---

## 3. 时空动力学的虚拟元方法

本文考虑小应变假设下的二维 ($d=2$) 弹性动力学问题。与有限元法相比，VEM 允许在空间域中使用非常任意的网格划分。此外，由于其形函数被投影到多项式空间，因此在空间域中只需要单点积分（对于 $k=1$）。这些特性使得 VEM 在求解此类时空动力学问题方面具有巨大优势。接下来，详细给出对式 (14)、(15) 和 (18) 的基本分析过程。

### 3.1 网格假设

本文选择时间张量积网格 $\{\mathcal{K}_h\}$ 用于时空柱体。如图 1 所示，空间域 $\Omega$ 被划分为许多不重叠的多边形 $\{\mathcal{K}_h^x\}$。一个多边形单元由面积 $|E|$、形心 $\mathbf{x}_E = (x_E, y_E)^T$ 和直径 $h_E$ 等参数定义。时间区间 $[0, T]$ 被划分为 $N_T$ 个子区间 $\mathcal{K}_{t_n} := (t_n, t_{n+1})$，节点为 $0 = t_0 < t_1 < \cdots < t_{N_T} = T$。$\{\mathcal{K}_h\}$ 中的每个单元 $\mathcal{K}$ 可以写成 $\mathcal{K}_x \times \mathcal{K}_t$，其中 $\mathcal{K}_x$ 是 $\{\mathcal{K}_h^x\}$ 中的二维网格。

### 3.2 投影算子

首先关注空间域 $\Omega$ 的离散形式。为简化和避免误解，对于 $\{\mathcal{K}_h^x\}$ 中的任意二维单元，我们选择 $E$ 代替 $\mathcal{K}_x$。

为了在空间域 $\Omega$ 中使用虚拟元方法，应首先介绍 VEM 中不同投影算子的定义。对于式 (18) 中使用的位移向量 $\pmb{u}$ 和速度 $\nu$，可以分别近似向量的分量（类似于有限元法），因此将引入以下 $\Pi^\nabla_{k,E}: V_1 \rightarrow \mathcal{P}_k(E)$ 投影算子：

$$
a_E(w - \Pi^\nabla_{k,E} w, q_k) = 0 \quad \forall q_k \in \mathcal{P}_k(E) \quad (20)
$$

其中 $k$ 是 VEM 的阶次（本文选择 $k=1$），$w$ 是任意标量，$a_E: V_1 \times V_1 \rightarrow \mathbb{R}$ 是双线性形式 $a(\cdot,\cdot)$ 的局部对应，$a_E(p,q) = \int_E \nabla p \cdot \nabla q \,\mathrm{d}\Omega$，$\forall p,q \in V_1$。为了求解上述投影算子，应添加额外的条件以固定 (20) 定义中的常数：

$$
\left\{
\begin{array}{ll}
\displaystyle\frac{1}{N_V^E}\sum_{i=1}^{N_V^E} \left(\Pi^\nabla_{k,E} w - w\right) = 0, & \text{对于 } k=1, \\[10pt]
\displaystyle\frac{1}{|E|} \int_E \left(\Pi^\nabla_{k,E} w - w\right) \mathrm{d}\Omega = 0, & \text{对于 } k>1,
\end{array}
\right. \quad (21)
$$

其中 $N_V^E$ 是顶点数。然后，虚拟元空间的实际定义可以给出为：

$$
V_E^h := \left\{ w \in V_1 : w|_{\partial E} \in \mathcal{B}(\partial E), \Delta w \in (\mathcal{P}_{k-2}(E))^3 \right\} \quad (22)
$$

其中 $\mathcal{B}(\partial E)$ 是边界单元空间：

$$
\mathcal{B}(\partial E) := \{ w \in C^0(\partial E) : w_F \in \mathcal{P}_k(F), \forall F \in \partial E \} \quad (23)
$$

函数空间的自由度选择为：
- 多边形单元 $E$ 所有 $N_V^E$ 个顶点上的 $w_h$ 值
- 对于 $k>1$，每条边 $e \in \partial E$ 上 $k-1$ 个 Gauss-Lobatto 求积点上的 $w_h$ 值
- 对于 $k>1$，直到 $k-2$ 阶的矩：
  $$
  \frac{1}{|E|}\int_E w_h q_a \,\mathrm{d}\Omega, \quad \forall q_a \in P_{k-2}(E)
  $$

将变量 $w_h$ 及其投影展开到不同空间，得到：

$$
w_h = \boldsymbol{\phi}^T \hat{w}_h, \quad \Pi^\nabla_{k,E} w_h = \mathbf{m}_k^T \boldsymbol{\Pi}^\nabla_{k,E^*} \hat{w}_h = \boldsymbol{\phi}^T \boldsymbol{\Pi}^\nabla_{k,E} \hat{w}_h \quad (24)
$$

其中 $\boldsymbol{\phi}$ 和 $\mathbf{m}_k$ 分别是 VEM 空间 $V_h^E$ 和多项式空间 $P_k$ 的基函数；$\boldsymbol{\Pi}^\nabla_{k,E^*}$ 是 Ritz 投影算子的矩阵形式，$\hat{\cdot}$ 表示节点值。将式 (24) 代入式 (21) 并考虑上述自由度，最终可以计算出 $H^1$ 投影算子。

通过观察式 (18) 的前三个公式，还需要另一个称为 $L^2$ 正交投影算子的投影。投影 $\Pi^0_{k,E}: L^2(E) \rightarrow P_k$ 定义为：

$$
\left(w - \Pi^0_{k,E} w, q_k\right)_E = 0, \quad \forall q_k \in P_k(E) \quad (25)
$$

可以选择 $V_h^E$ 中相同的自由度。考虑到 [49] 中的增强 VE 空间，可以计算出 $L^2$ 正交投影算子。可以验证，对于 $k \leq 2$，有 $\Pi^\nabla_{k,E} = \Pi^0_{k,E}$。本文考虑 $k=1$ 的情况，因此不需要额外计算 $L^2$ 正交投影算子。

### 3.3 空间域中的离散双线性形式

由于采用了时间张量积网格 $\kappa = \kappa_x \times \kappa_t$（空间域 $\Omega$ 中的虚拟元和时域中的拉格朗日插值），可以首先计算空间域 $\Omega$ 中单元的双线性格式 $a^E(p_h, q_h)$，然后对时间积分以计算最终的双线性格式 $A^E(p_h, q_h)$。本部分考虑空间域中的积分计算。

首先，对于标量场方程的一般形式，近似 $a_h^E, m_h^E$ 可以基于上述定义的投影计算：

$$
\begin{array}{l}
a_h^E(p_h, q_h) := a^E(\Pi^\nabla_{k,E} p_h, \Pi^\nabla_{k,E} q_h) + S^E\left((I - \Pi^\nabla_{k,E})\hat{p}_h, (I - \Pi^\nabla_{k,E})\hat{q}_h\right), \\[5pt]
m_h^E(p_h, q_h) := m^E(\Pi^0_{k,E} p_h, \Pi^0_{k,E} q_h) + R^E\left((I - \Pi^0_{k,E})\hat{p}_h, (I - \Pi^0_{k,E})\hat{q}_h\right),
\end{array} \quad (26)
$$

其中 $S^E$ 和 $R^E$ 是稳定化双线性形式，$I$ 是单位矩阵。可以看出，我们的双线性格式需要额外的稳定化项。基于上述定义，以下有用的积分可以计算为：

$$
\begin{aligned}
a_h^E(p,q) &= \int_{\kappa_x} \left(\nabla \Pi^\nabla_{k,E} p_h\right) \cdot \left(\nabla \Pi^\nabla_{k,E} q_h\right) \mathrm{d}\Omega + S^E\left((I - \boldsymbol{\Pi}^\nabla_{k,E})\hat{p}_h, (I - \boldsymbol{\Pi}^\nabla_{k,E})\hat{q}_h\right) \\
&= \hat{p}_h^T \left[ (\boldsymbol{\Pi}^\nabla_{k,E^*})^T \int_{\kappa_x} \nabla \mathbf{m}_k \nabla \mathbf{m}_k^T \mathrm{d}\Omega \,\boldsymbol{\Pi}^\nabla_{k,E^*} + (I - \boldsymbol{\Pi}^\nabla_{k,E})^T (I - \boldsymbol{\Pi}^\nabla_{k,E}) \right] \hat{q}_h \\
&= \hat{p}_h^T (\mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla) \hat{q}_h = \hat{p}_h^T \mathbf{K}^\nabla \hat{q}_h,
\end{aligned} \quad (28)
$$

$$
\begin{aligned}
m_h^E(p,q) &= \int_{\kappa_x} \left(\Pi^0_{k,E} p_h\right) \cdot \left(\Pi^0_{k,E} q_h\right) \mathrm{d}\Omega + R^E\left((I - \boldsymbol{\Pi}^0_{k,E})\hat{p}_h, (I - \boldsymbol{\Pi}^0_{k,E})\hat{q}_h\right) \\
&= \hat{p}_h^T \left[ (\boldsymbol{\Pi}^0_{k,E^*})^T \int_{\kappa_x} \mathbf{m}_k \mathbf{m}_k^T \mathrm{d}\Omega \,\boldsymbol{\Pi}^0_{k,E^*} + |E| (I - \boldsymbol{\Pi}^0_{k,E})^T (I - \boldsymbol{\Pi}^0_{k,E}) \right] \hat{q}_h \\
&= \hat{p}_h^T (\mathbf{K}_c^0 + \mathbf{K}_s^0) \hat{q}_h = \hat{p}_h^T \mathbf{K}^0 \hat{q}_h,
\end{aligned} \quad (29)
$$

其中：

$$
\begin{aligned}
\mathbf{K}_c^0 &:= (\boldsymbol{\Pi}^0_{k,E^*})^T \int_{\kappa_x} \mathbf{m}_k \mathbf{m}_k^T \mathrm{d}\Omega \,\boldsymbol{\Pi}^0_{k,E^*}, \\
\mathbf{K}_s^0 &:= |E| (I - \boldsymbol{\Pi}^0_{k,E})^T (I - \boldsymbol{\Pi}^0_{k,E}), \\
\mathbf{K}^0 &:= \mathbf{K}_c^0 + \mathbf{K}_s^0,
\end{aligned} \quad (30)
$$

$$
\begin{aligned}
\mathbf{K}_c^\nabla &:= (\boldsymbol{\Pi}^\nabla_{k,E^*})^T \int_{\kappa_x} \nabla \mathbf{m} \nabla \mathbf{m}^T \mathrm{d}\Omega \,\boldsymbol{\Pi}^\nabla_{k,E^*}, \\
\mathbf{K}_s^\nabla &:= (I - \boldsymbol{\Pi}^\nabla_{k,E})^T (I - \boldsymbol{\Pi}^\nabla_{k,E}), \\
\mathbf{K}^\nabla &:= \mathbf{K}_c^\nabla + \mathbf{K}_s^\nabla.
\end{aligned} \quad (31)
$$

接下来，考虑向量场 $\pmb{u}$ 和 $\nu$ 的双线性形式。例如，位移可以写成以下形式：

$$
\pmb{u} = [u_x, u_y]^T, \quad \delta\pmb{u} = [\delta u_x, \delta u_y]^T \quad (32)
$$

则速度 $\nu$ 的以下双线性形式可以计算为：

$$
m_h^E(\delta\nu, \nu) = \int_{\kappa_x} \delta\nu \cdot \nu \,\mathrm{d}\Omega = \begin{bmatrix} \delta\hat{\nu}_x^T & \delta\hat{\nu}_y^T \end{bmatrix} \begin{bmatrix} \mathbf{K}^0 & \\ & \mathbf{K}^0 \end{bmatrix} \begin{bmatrix} \hat{\nu}_x \\ \hat{\nu}_y \end{bmatrix} = \delta\hat{\nu}^T \mathbb{K}^0 \hat{\nu}, \quad \mathbb{K}^0 := \begin{bmatrix} \mathbf{K}^0 & \\ & \mathbf{K}^0 \end{bmatrix} \quad (33)
$$

使用 Voigt 记号 $(\tilde{\square})$ 表示应变：

$$
\tilde{\boldsymbol{\varepsilon}}(\pmb{u}) = \mathbf{A} \mathbf{d}(\pmb{u}) \quad (34)
$$

其中：

$$
\mathbf{A} = \begin{bmatrix}
1 & 0 & 0 & 0 \\
0 & 0 & 0 & 1 \\
0 & 1 & 1 & 0
\end{bmatrix}, \quad
\mathbf{d}(\pmb{u}) = \begin{bmatrix}
\frac{\partial u_x}{\partial x} & \frac{\partial u_x}{\partial y} & \frac{\partial u_y}{\partial x} & \frac{\partial u_y}{\partial y}
\end{bmatrix}^T \quad (35)
$$

则：

$$
\Pi^\nabla_{k,E} \pmb{u}_h = \begin{bmatrix} \mathbf{m}^T & \\ & \mathbf{m}^T \end{bmatrix} \begin{bmatrix} \boldsymbol{\Pi}^\nabla_{k,E^*} & \\ & \boldsymbol{\Pi}^\nabla_{k,E^*} \end{bmatrix} \begin{bmatrix} \hat{u}_h^x \\ \hat{u}_h^y \end{bmatrix} = \begin{bmatrix} \boldsymbol{\phi}^T & \\ & \boldsymbol{\phi}^T \end{bmatrix} \begin{bmatrix} \boldsymbol{\Pi}^\nabla_{k,E} & \\ & \boldsymbol{\Pi}^\nabla_{k,E} \end{bmatrix} \begin{bmatrix} \hat{u}_h^x \\ \hat{u}_h^y \end{bmatrix} = \mathbf{M}^T \boldsymbol{\Pi}^\nabla_* \hat{\pmb{u}}_h \quad (36)
$$

考虑之前的定义，应变的双线性形式可以写为：

$$
\begin{aligned}
a_h^E(\delta\pmb{u}, \pmb{u}) &= \int_{\kappa_x} \varepsilon(\delta\pmb{u}) : \mathbb{D} : \varepsilon(\pmb{u}) \,\mathrm{d}\Omega = \int_{\kappa_x} \mathbf{d}(\delta\pmb{u})^T \mathbf{A}^T \tilde{\mathbf{D}} \mathbf{A} \mathbf{d}(\pmb{u}) \,\mathrm{d}\Omega \\
&= \delta\hat{\pmb{u}}^t \left( (\boldsymbol{\Pi}^\nabla_*)^T \int_{\kappa_x} \nabla \mathbf{M} \mathbf{A}^T \tilde{\mathbf{D}} \mathbf{A} \nabla \mathbf{M}^T \mathrm{d}\Omega \,\boldsymbol{\Pi}^\nabla_* + \gamma \begin{bmatrix} \mathbf{K}_s^\nabla & \\ & \mathbf{K}_s^\nabla \end{bmatrix} \right) \hat{\pmb{u}} \\
&= \delta\hat{\pmb{u}}^t (\mathbf{K}_c^\varepsilon + \gamma \mathbf{K}_s^\varepsilon) \hat{\pmb{u}} = \delta\hat{\pmb{u}}^t \mathbf{K}^\varepsilon \hat{\pmb{u}},
\end{aligned} \quad (37)
$$

其中式 (37) 中的 $\gamma$ 是稳定化参数，可以选择为 $\gamma = \frac{1}{4N_K} \mathrm{tr}(\mathbf{K}_c^\varepsilon)$。稳定化项可以有不同选择，详细信息参见相关工作 [4,24]。通常，对于线弹性问题，稳定化项中参数的选择对结果影响很小。对于线弹性问题，矩阵 $\mathbf{K}_c^\varepsilon$ 也可以基于 $\mathbf{K}_c^\nabla$ 获得。在实际编程中，可以使用式 (37) 中的形式直接计算矩阵。矩阵 $\mathbf{K}^0$、$\mathbb{K}^0$、$\mathbf{K}^\varepsilon$（式 (33)-(37)）可用于计算时空动力学问题的刚度矩阵。

### 3.4 线性时空弹性的矩阵形式

由于空间域 $\Omega$ 被划分为一组不重叠的多边形 $\{\mathcal{T}_h^s\}$，可以基于 Gauss-Legendre 求积计算式 (33)-(37) 的积分。对于本文讨论的一阶格式，可以执行单点积分。时间区间 $[0, T]$ 被划分为 $N_T$ 个子区间 $\mathcal{K}_{t_n} := (t_n, t_{n+1})$，节点为 $0 = t_0 < t_1 < \cdots < t_{N_T} = T$。然后，时空单元可以表示为 $\mathcal{K} = \mathcal{K}_x \times \mathcal{K}_t$。

在当前假设下，时空单元的上下表面具有相同的形状，因此它们的形函数相等。如果假设时间维度为线性插值，则时空单元 $\mathcal{K}$ 的形函数形式为：

$$
\Phi_{\mathcal{K}} = \begin{bmatrix} \Phi_{\mathcal{K}_x^b} \Phi_{\mathcal{K}_t}(1) \\ \Phi_{\mathcal{K}_x^t} \Phi_{\mathcal{K}_t}(2) \end{bmatrix} \quad (38)
$$

其中 $\Phi_{\mathcal{K}_x^b} = \Phi_{\mathcal{K}_x^t} =: \Phi_{\mathcal{K}_x}$ 是虚拟元的形函数，$\Phi_{\mathcal{K}_t} := [\Phi_{\mathcal{K}_t}(1), \Phi_{\mathcal{K}_t}(2)]^T$ 是时间方向上一维线单元的基本函数。形函数为：

$$
\Phi_{\mathcal{K}_t}(1) = \frac{1 - \xi}{2}, \quad \Phi_{\mathcal{K}_t}(2) = \frac{1 + \xi}{2} \quad (39)
$$

张量积形函数的梯度形式为：

$$
\nabla_x \Phi_{\mathcal{K}} = \begin{bmatrix} \nabla_x \Phi_{\mathcal{K}_x^b} \Phi_{\mathcal{K}_t}(1) \\ \nabla_x \Phi_{\mathcal{K}_x^t} \Phi_{\mathcal{K}_t}(2) \end{bmatrix}, \quad \nabla_t \Phi_{\mathcal{K}} = \begin{bmatrix} \Phi_{\mathcal{K}_x^b} \nabla_t \Phi_{\mathcal{K}_t}(1) \\ \Phi_{\mathcal{K}_x^t} \nabla_t \Phi_{\mathcal{K}_t}(2) \end{bmatrix} \quad (40)
$$

时空单元的位移 $\pmb{U}$ 由下式给出：

$$
U_h = \begin{bmatrix} \Phi_{\mathcal{K}}^T & \\ & \Phi_{\mathcal{K}}^T \end{bmatrix} \hat{U}_h = \Phi_{\mathcal{K}}^T \hat{U}_h \quad (41)
$$

其中：

$$
\Phi_{\mathcal{K}}^T := \begin{bmatrix} \Phi_{\mathcal{K}}^T & \\ & \Phi_{\mathcal{K}}^T \end{bmatrix}, \quad \hat{U}_h := \begin{bmatrix} \hat{u}_x^b & \hat{u}_x^t & \hat{u}_y^b & \hat{u}_y^t \end{bmatrix}^T \quad (42)
$$

速度 $\pmb{v}$ 使用相同的插值函数。

我们的离散格式基于式 (18)。对于每个时空单元 $\mathcal{K}$，单元刚度矩阵 $\mathbf{K}$ 具有分块形式：

$$
\mathbf{K} := \begin{bmatrix} \mathbf{K}_{vv} & \mathbf{K}_{vu} \\ \mathbf{K}_{uv} & \mathbf{K}_{uu} \end{bmatrix} \quad (43)
$$

根据之前计算的矩阵，可以得到以下矩阵：

$$
\begin{aligned}
\mathbf{K}_{vv} &= \rho^{-1} \int_{t_n}^{t_{n+1}} \int_{\mathcal{K}_x} (\Phi_{\mathcal{K}} + \theta h \nabla_t \Phi_{\mathcal{K}}) \Phi_{\mathcal{K}}^T \,\mathrm{d}\Omega \mathrm{d}t \\
&= \rho^{-1} \begin{bmatrix} \int_{t_n}^{t_{n+1}} \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t \otimes \int_{\mathcal{K}_x} \phi_{\mathcal{K}_x} \phi_{\mathcal{K}_x}^T \mathrm{d}\Omega & \\ & \int_{t_n}^{t_{n+1}} \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t \otimes \int_{\mathcal{K}_x} \phi_{\mathcal{K}_x} \phi_{\mathcal{K}_x}^T \mathrm{d}\Omega \end{bmatrix} \\
&\quad + \rho^{-1} \theta h \begin{bmatrix} \int_{t_n}^{t_{n+1}} \nabla_t \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t \otimes \int_{\mathcal{K}_x} \phi_{\mathcal{K}_x} \phi_{\mathcal{K}_x}^T \mathrm{d}\Omega & \\ & \int_{t_n}^{t_{n+1}} \nabla_t \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t \otimes \int_{\mathcal{K}_x} \phi_{\mathcal{K}_x} \phi_{\mathcal{K}_x}^T \mathrm{d}\Omega \end{bmatrix}
\end{aligned} \quad (44)
$$

其中 $\otimes$ 是 Kronecker 积，定义为 $\mathbf{A} \otimes \mathbf{B} = (a_{ij} \mathbf{B})$，$\mathbf{A} = (a_{ij})$。式 (44) 中沿空间域的积分项之前已计算为：

$$
\int_{\mathcal{K}_x} \phi_{\mathcal{K}_x} \phi_{\mathcal{K}_x}^T \mathrm{d}\Omega = \mathbf{K}_c^0 + \mathbf{K}_s^0 =: \mathbf{K}^0 \quad (45)
$$

其中 $\mathbf{K}_c^0$ 和 $\mathbf{K}_s^0$ 在式 (30) 中定义。

由于选择拉格朗日插值来表示 $\phi_{\mathcal{K}_t}$，沿时间方向的积分项可以基于高斯积分计算：

$$
\int_{t_n}^{t_{n+1}} \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t = \frac{h}{2} \int_{-1}^{1} \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}\xi = \frac{h}{2} \sum_{i=1}^{2} w(i) \phi_{\mathcal{K}_t}(\xi_i) \phi_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{L} \quad (46)
$$

$$
\int_{t_n}^{t_{n+1}} \nabla_t \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}t = \frac{h}{2} \int_{-1}^{1} \nabla_t \phi_{\mathcal{K}_t} \phi_{\mathcal{K}_t}^T \mathrm{d}\xi = \frac{h}{2} \sum_{i=1}^{2} w(i) \nabla_t \phi_{\mathcal{K}_t}(\xi_i) \phi_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{Q} \quad (47)
$$

$$
\int_{t_n}^{t_{n+1}} \phi_{\mathcal{K}_t} \nabla_t \phi_{\mathcal{K}_t}^T \mathrm{d}t = \frac{h}{2} \int_{-1}^{1} \phi_{\mathcal{K}_t} \nabla_t \phi_{\mathcal{K}_t}^T \mathrm{d}\xi = \frac{h}{2} \sum_{i=1}^{2} w(i) \phi_{\mathcal{K}_t}(\xi_i) \nabla_t \phi_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{R} \quad (48)
$$

$$
\int_{t_n}^{t_{n+1}} \nabla_t \phi_{\mathcal{K}_t} \nabla_t \phi_{\mathcal{K}_t}^T \mathrm{d}t = \frac{h}{2} \int_{-1}^{1} \nabla_t \phi_{\mathcal{K}_t} \nabla_t \phi_{\mathcal{K}_t}^T \mathrm{d}\xi = \frac{h}{2} \sum_{i=1}^{2} w(i) \nabla_t \phi_{\mathcal{K}_t}(\xi_i) \nabla_t \phi_{\mathcal{K}_t}^T(\xi_i) =: \mathbf{W} \quad (49)
$$

其中 $h$ 是时空虚拟元在时间方向上的尺寸，$w(i)$ 是高斯积分的权重。

如果将任意矩阵 $\mathbf{K}_{any}$ 定义为如下的分块矩阵：

$$
\mathbf{K}_{any} = \begin{bmatrix} \mathbf{K}_{any}^{I,I} & \mathbf{K}_{any}^{I,II} \\ \mathbf{K}_{any}^{II,I} & \mathbf{K}_{any}^{II,II} \end{bmatrix} \quad (50)
$$

则矩阵 $\mathbf{K}_{vv}$ 也可以写为：

$$
\mathbf{K}_{vv} = \rho^{-1} \begin{bmatrix} \mathbf{L} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{L} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} + \rho^{-1} \theta h \begin{bmatrix} \mathbf{Q} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{Q} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} \quad (51)
$$

其中 $\mathbb{K}^0 := \operatorname{diag}(\mathbf{K}^0, \mathbf{K}^0)$ 如式 (33) 所定义。

此外，其他刚度矩阵可以如下计算：

$$
\mathbf{K}_{vu} = \begin{bmatrix} \mathbf{R} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{R} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} + \theta h \begin{bmatrix} \mathbf{W} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{W} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} \quad (52)
$$

$$
\mathbf{K}_{uv} = \begin{bmatrix} \mathbf{R} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{R} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} + \theta h \begin{bmatrix} \mathbf{W} \otimes \mathbb{K}^0_{I,I} & \\ & \mathbf{W} \otimes \mathbb{K}^0_{II,II} \end{bmatrix} \quad (53)
$$

$$
\mathbf{K}_{uu} = \begin{bmatrix} \mathbf{L} \otimes \mathbf{K}^\varepsilon_{I,I} & \mathbf{L} \otimes \mathbf{K}^\varepsilon_{I,II} \\ \mathbf{L} \otimes \mathbf{K}^\varepsilon_{II,I} & \mathbf{L} \otimes \mathbf{K}^\varepsilon_{II,II} \end{bmatrix} + \theta h \begin{bmatrix} \mathbf{Q} \otimes \mathbf{K}^\varepsilon_{I,I} & \mathbf{Q} \otimes \mathbf{K}^\varepsilon_{I,II} \\ \mathbf{Q} \otimes \mathbf{K}^\varepsilon_{II,I} & \mathbf{Q} \otimes \mathbf{K}^\varepsilon_{II,II} \end{bmatrix} \quad (54)
$$

其中 $\mathbb{K}^0$ 和 $\mathbf{K}^\varepsilon$ 在式 (33) 和 (37) 中给出。通过组装所有单元并求解方程组，可以直接获得不同时刻的位移 $\pmb{u}$，而无需任何时间积分格式。由于选择了时间张量积网格，刚度矩阵 $\mathbf{K}$ 的计算非常高效（只需计算二维计算域中单元的某些积分 $\{\mathcal{K}_h^x\}$，然后通过 Kronecker 积计算总体刚度矩阵）。

为了阐明分析过程，给出了流程图，如图 3 所示。

---

## 4. 时空动力学的有限元方法

式 (51)-(54) 的形式提供了一个统一的格式，因此可以用于推导具有时间张量积网格的有限元法来计算时空动力学。与虚拟元方法类似，需要基于有限元法计算二维计算域 $\Omega$ 中单元的矩阵 $\mathbf{K}^0$ 和 $\mathbf{K}^\varepsilon$，然后基于式 (51)-(54) 计算刚度矩阵。本文选择一阶四边形单元进行空间域的离散化。在时间方向上，使用两节点线性单元。

使用有限元法的形函数 $\phi_{\mathrm{FEM}}$，则矩阵 $\mathbf{K}^0$ 和 $\mathbf{K}^\varepsilon$ 可以计算为：

$$
\mathbf{K}^0 = \int_{\kappa_x} [\phi_{\mathrm{FEM}}] [\phi_{\mathrm{FEM}}]^T \mathrm{d}\Omega \quad (56)
$$

$$
\mathbf{K}^\varepsilon = \int_{\kappa_x} \mathbf{B}^T \hat{\mathbf{D}} \mathbf{B} \,\mathrm{d}\Omega \quad (57)
$$

其中：

$$
\mathbf{B} := \begin{bmatrix}
\frac{\partial \phi_{\mathrm{FEM}}^T}{\partial x} & \mathbf{0}^T \\
\mathbf{0}^T & \frac{\partial \phi_{\mathrm{FEM}}^T}{\partial y} \\
\frac{\partial \phi_{\mathrm{FEM}}^T}{\partial y} & \frac{\partial \phi_{\mathrm{FEM}}^T}{\partial x}
\end{bmatrix} \quad (58)
$$

将式 (56) 和 (58) 代入式 (52)-(54)，得到刚度矩阵。与传统有限元法中的时间积分格式相比，该格式具有高计算效率和编程简单的特点。相关的有限元代码可在 https://github.com/Qinxiaoye/VEM-space-time-dynamic 或 https://www.vemhub.com/code 找到。

---

## 5. 数值算例

### 5.1 横向梁振动

第一个算例的目的是研究稳定性参数 $\theta$ 对时空格式结果的影响。如图 4 所示，考虑一个悬臂梁，形状为 $L \times H = 5 \times 1$。左侧固定，右侧施加两种不同的垂直载荷 $P(t)$（见图 4）。第一种是恒定载荷 $P = P_0$。另一种是半正弦载荷 $P = P_0 \sin(\pi t/5)$，$t \leq 5$。材料参数假设为 $E = 200000$，$\nu = 0.3$。

由于计算域 $\Omega$ 使用虚拟元方法离散化，可以使用多边形网格（包括非凸网格），如图 4 所示。在本算例中，选择有限元法进行比较。除了使用时空方法外，还使用 Newmark 方法求解动力学问题以进行比较。对于不同的时间步长和不同的网格，时空方案中使用的总自由度列于表 1。

首先，假设恒定载荷 $P = P_0 = 100$。为了测试稳定化参数 $\theta$ 的影响，使用网格 1（如图 4 所示）的虚拟元方法。密度选择为 $\rho = 100$。时间方向上的单元长度选择为 $\Delta t = 0.2$，总计算时间为 10 秒。参考解通过 Newmark 时间积分方法获得，时间步长为 $\Delta t = 0.01$。对于不同的稳定化参数 $\theta \in \{0, 0.01, 0.1, 0.4, 0.8\}$，右下节点在 $y$ 方向的位移随时间变化如图 5 所示。在给定的范围内，参数 $\theta$ 对结果影响很小。一般来说，较大的 $\theta$ 更稳定，但解会被阻尼，导致能量损失和位移减小。同时，较小的 $\theta$ 会产生更好的结果。因此，在后续分析中选择 $\theta = 0.01$。

由于 Newmark 方法不是严格能量守恒的，在长期积分中可能会发生能量漂移。为了测试虚拟元方法的时空方案，选择梁的密度为 $\rho = 10$。时间方向上的单元长度选择为 $\Delta t = 0.1$，总计算时间为 10 秒。同时，在 FEM 和 VEM 中使用 Newmark 方法（$\Delta t = 0.1$）计算位移。右下节点的 $y$ 方向位移如图 6 所示。可以看出，与 Newmark 方法相比，时空方案在使用相同时间步长时没有相位误差。

除了恒定载荷外，还可以引入时变载荷条件。如图 4 所示，假设半正弦载荷 $P = P_0 \sin(\pi t/5)$，$P_0 = 100$，$t \leq 5$（$t > 5$ 时 $P_0 = 0$）。时间方向上的单元长度选择为 $\Delta t = 0.1$，总计算时间为 10 秒。使用非常小的时间步长（$\Delta t = 0.01$）的有限元法作为比较。右下节点的 $y$ 方向位移如图 7 所示。可以看出，在当前情况下，时空 VEM 仍然可以获得足够精确的结果，而 Newmark 方法获得的结果对时间步长要求更敏感。

计算效率是我们关注的一个重要问题。对于相同的时空框架，选择 VEM 和 FEM 进行比较。对于四边形网格，当时间步长为 $\Delta t = 0.2$ 时，VEM 耗时 2.46 秒，FEM 耗时 2.57 秒。当时间步长为 $\Delta t = 0.1$ 时，VEM 耗时 5.33 秒，FEM 耗时 5.76 秒。可以看出，虚拟元方法的效率略快于有限元方法。由于时空方案增加了问题的自由度，它在求解线性代数方程上花费了很大比例的时间。这在 VEM 和 FEM 中都可以观察到。当时间步长为 $\Delta t = 0.1$ 时，VEM 求解方程耗时 3.503 秒，FEM 耗时 4.13 秒。

通过时空 VEM 获得的位移 $u_y$ 和速度 $v_y$ 的云图，使用不同网格和不同密度，分别如图 8 和图 9 所示。

### 5.2 Cook 膜问题

在本算例中，考虑 Cook 膜，如图 10 所示。相关尺寸为 $L = 48$，$H_1 = 44$，$H_2 = 16$。膜左侧固定，右侧施加恒定的垂直载荷 $q_y = 100$。在本算例中，使用了两种不同类型的网格，包括规则网格和多边形网格（如图 10 所示）。材料参数假设为 $E = 200000$，$\nu = 0.3$，$\rho = 10$。

为了测试时空 VEM 的收敛性，给出了网格细化研究，单元划分为 $2^N \times 2^N$，$N = 2, 3, 4$。对于时空 VEM，时间方向上的网格长度选择为 $h = \Delta t = 0.2$ 和 $h = \Delta t = 0.1$。此外，还使用 Newmark 方法进行比较。对于不同的时间步长和不同的网格，时空方案中使用的总自由度列于表 2。

右上角最大位移 $u_y$ 的收敛性研究如图 11 所示。可以观察到，随着网格尺寸的减小，时空 VEM 获得的结果收敛到参考解。对于 $N = 4$，即使 $\Delta t = 0.2$，位移也与参考解非常一致。

时空域的 $y$ 方向位移云图和 von Mises 应力云图（$N = 4$，$\Delta t = 0.2$）如图 12 所示。对于 $N = 4$，使用不同网格获得的 von Mises 应力云图如图 13 所示。

### 5.3 中心斜裂纹板

在本算例中，考虑一个具有任意方向中心裂纹的矩形板，如图 14 所示。板的几何参数为 $2H = 40$，$2L = 20$，裂纹长度为 $2a = 5$。在板的顶部和底部施加均匀拉力 $P = 100$。材料参数假设为 $E = 200000$，$\nu = 0.3$，$\rho = 10$。中心裂纹可以旋转到任意角度 $\alpha$，本文选择不同的角度 $\alpha = 0^\circ, 20^\circ, 40^\circ, 60^\circ, 80^\circ$。这是 VEM 的一个优势，因为界面上的单元可以具有任意数量的节点。因此，图 14 中旋转的内部网格始终保持连续的离散化。

使用时空方案，时间方向上的网格尺寸选择为 $\Delta t = 0.02$。同样，使用时间步长 $\Delta t = 0.01$ 的 Newmark 方法进行比较。对于不同的角度，节点总数不同。不同角度的总自由度列于表 3。对于不同角度，右上角位移 $u_y$ 随时间变化如图 15 所示。时空 VEM（$\Delta t = 0.02$）获得的位移与 Newmark 方法（$\Delta t = 0.01$）获得的位移非常一致。不同角度的 von Mises 应力和位移 $u_y$ 的云图如图 16 和图 17 所示。$t = 0.8$ 时不同 $\alpha$ 的 von Mises 应力如图 18 所示。

### 5.4 考虑体力的任意几何形状

最后一个算例考虑一个受体积力作用的二维动物形状板。几何形状和多边形 VEM 网格如图 19 所示。本文考虑向下的和向右的体力 $g = 10$。材料参数假设为 $E = 200000$，$\nu = 0.3$，$\rho = 50$。

使用时空虚拟元方法，时间方向上的网格尺寸 $h = \Delta t = 2$。总计算时间为 $t = 80$。总自由度为 222876。对于时空域 $\Omega \times I$，两种不同体力下的 von Mises 应力如图 20 所示。此外，$t = 80$ 时两种不同体力下的 von Mises 应力如图 21 所示。

---

## 6. 结论

本文基于哈密顿作用量驻值原理，提出了一种用于二维弹性动力学离散化的时空虚拟元方法。在我们的时空方案中，空间维度使用虚拟元方法，时间维度使用有限元方法。与其他时空公式相比，"结构化"数值方法避免了在时间方向上引入额外的自由度，简化了计算。此外，空间维度可以使用任意多边形单元，从而增强了网格划分的灵活性。新的时空公式具有高精度和稳定性的优点。对于大规模问题，计算效率可能较慢，因此需要并行技术和自适应网格。时空方案将在未来扩展到非线性问题。本工作中涉及的虚拟元方法代码和有限元方法代码可在 https://github.com/Qinxiaoye/VEM-space-time-dynamic 找到。

---

## 致谢

第一作者和最后作者感谢德国亚历山大·冯·洪堡基金会的支持。

---

## 参考文献

[1] L. Beirão da Veiga, A. Cangiani, G. Manzini, L. Marini, A. Russo, Basic principles of virtual element methods, Math. Models Methods Appl. Sci. 23 (2012) 199-214.

[2] L. Beirão da Veiga, F. Brezzi, L. Marini, A. Russo, Mixed virtual element methods for general second order elliptic problems on polygonal meshes, ESAIM Math. Model. Numer. Anal. 26 (2014) 727-747.

[3] L. Beirão da Veiga, F. Brezzi, Virtual elements for linear elasticity problems, SIAM J. Numer. Anal. 51 (2) (2013) 794-812.

...（其余参考文献略）